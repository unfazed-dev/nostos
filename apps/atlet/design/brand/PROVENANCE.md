# input/design/brand — design brand freeze (raster set)

GENERATED by `python3 pipeline/tools/freeze_brand.py` — operator-run brand freeze
(never inside `crew run`; the pipeline only byte-copies these committed bytes).
Regenerate only when the design's logo changes, then re-commit.

- Component: `AtletMark` (auto-detected /mark|logo/i SVG arrow component in the design shards)
- JSX -> SVG mechanically: comments/style stripped, prop defaults substituted,
  `var(--*)` resolved from styles.css (`:root` light / `[data-theme="dark"]` dark),
  light color literals re-palettized to their dark var values for the dark variant,
  React SVG attrs mapped to kebab-case.
- Light literals resolved: #1A1714, #D2522B
- Rasterized with: rsvg-convert version 2.62.3
- logoSize 80 (startup.json — the design splash's authored logical size): the
  splash slots render the mark at EXACTLY that pt/dp size (logo-splash = 320px
  = @4x source; logo-android12 = 1152px canvas with the mark at that same 320px,
  centered inside the 768px circle mask — never enlarged to fill it).
- Slots: logo-1024.png, logo-dark-1024.png, icon-512.png, icon-192.png, icon-192-dark.png, favicon-32.png, logo-splash.png, logo-splash-dark.png, logo-android12.png, logo-android12-dark.png (dark slots fall back to the light
  variant when a design has no dark palette block).

Contract (13-verify scaffold): this directory is ALL-OR-NOTHING — complete set =>
design brand replaces the asko default for EVERY brand slot (native splash,
StartupView, launcher icons, web icons/favicon/loader); partial => loud fail;
absent => asko default brand (pipeline/assets/brand).
