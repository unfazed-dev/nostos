[package]
name = "nostos-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Nostos client sync core — the platform-agnostic apply engine (pure, no tokio, no SQLite)."
repository.workspace = true
homepage.workspace = true

[features]
# The cross-platform conformance suite. Off by default so the cases never ship
# in a device binary; `nostos-client` (and, once the bridge exposes it,
# `nostos-ffi-wasm`) turn it on as a dev-dependency.
conformance = []

[dependencies]
nostos-domain = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
bytes = { workspace = true }

[lints]
workspace = true
