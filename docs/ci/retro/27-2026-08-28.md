# [arxa-builder][arxa-tester] 2026-08-28 — no-credentials device round-trip, tenant-column audit, tenant-scoped CRDT merge

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder · `[arxa-tester]` arxa-tester

**Date:** 2026-08-28 (historical — replayed as retro PR #27; the PR page shows the replay date)

**Branch:** `retro/27-2026-08-28` · **merge:** `9cddbd3` · **commits:** 17

## What is now true

- nostos_flutter: export the AttachmentDatabase extension from the barrel
- atlet: no-credentials device round-trip harness — offline to online delta apply, green on iPhone and Android emulator
- nostos_flutter: export the NostosEngine seam from the barrel
- infra: tenant-scoped snapshots skip tables without the tenant column — deliberately-global tables 42703ed and starved
- infra: snapshot query errors render their full source chain
- client: rejected subscribes surface as fatal errors and connected now means proven
- server: boot-time tenant-column audit names columnless and missing synced tables
- cli: nostos dev forwards CRDT column declarations from .env
- infra: tenant-scoped CRDT merge — ADR-0018 extended to the ADR-0030 paths
- flutter: NostosDatabase.supabase() opens sessionless — sync starts at sign-in
- …and 1 more — see Commits

## Commits

### Features

- *(atlet)* no-credentials device round-trip harness — offline to online delta apply, green on iPhone and Android emulator (`1061df4`)
- *(server)* boot-time tenant-column audit names columnless and missing synced tables (`dcbff99`)
- *(infra)* tenant-scoped CRDT merge — ADR-0018 extended to the ADR-0030 paths (`2b54757`)
- *(flutter)* NostosDatabase.supabase() opens sessionless — sync starts at sign-in (`d57a855`)

### Bug Fixes

- *(nostos_flutter)* export the AttachmentDatabase extension from the barrel (`9dc9060`)
- *(nostos_flutter)* export the NostosEngine seam from the barrel (`530eb88`)
- *(infra)* tenant-scoped snapshots skip tables without the tenant column — deliberately-global tables 42703ed and starved (`b7551ef`)
- *(infra)* snapshot query errors render their full source chain (`266edea`)
- *(client)* rejected subscribes surface as fatal errors and connected now means proven (`581b9bc`)
- *(cli)* nostos dev forwards CRDT column declarations from .env (`aa5ee27`)
- *(client)* reset_subscribed via mem::take — clippy if_not_else (`5ff6c1a`)

### Documentation

- plan status addendum — M7 minting seam closed, send path mapped, ADR-0041 pending (`d5721bb`)

### Styling

- rustfmt drift from tenant-CRDT commits (`6bb730d`)
- clippy pedantic cleanups — fn-pointer take, nested or-pattern (`45c3aa2`)

### Testing

- *(flutter)* integration harness compiles server first with tunable budget and isolates ambient nostos_rules.toml (`4562c8d`)
- *(infra)* ADR-0040 reorder regression — oversized snapshot drains, cap reject closes 1000 (`e7ac28f`)

### Miscellaneous Tasks

- *(flutter)* frb regen — doc-comment sync only, wire glue byte-identical (`34f992f`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
