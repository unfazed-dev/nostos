# [arxa-builder][arxa-designer][arxa-tester] 2026-09-22 — honest conflation benchmarks; direct mode over PostgREST and Realtime

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder · `[arxa-designer]` arxa-designer · `[arxa-tester]` arxa-tester

**Date:** 2026-09-22 (historical — replayed as retro PR #36; the PR page shows the replay date)

**Branch:** `retro/36-2026-09-22` · **merge:** `66f5ef1` · **commits:** 40

## What is now true

- fanout: DeliveryDecision::Superseded — a conflated frame is convergent, not lost, and drop% now subtracts it
- bench: a timed-out run reports no ops/sec or drop% — the 120s default silently corrupted the 1k tier on a 4-core host
- bench: finish on quiescence, not a loss-free target — any shed event made the old wait loop spin to the deadline and dilute ops/sec
- bench: open-loop --rate, a fixed repetition policy, and randomised run order — the last three harness defects
- bench: plot every repetition in execution order — a step is the machine changing, which no mean, median or spread can say
- bench: a per-run delivery budget so every tier moves the same frames
- core: direct-mode pull cursor — group by xid, hold back a page-cut transaction, persist the xid8 horizon
- client: postgrest change source for direct mode — page by transaction, drain to the horizon
- client: realtime private-channel doorbell for direct mode, with the documented join/system error split
- client: direct-mode write path — three plain postgrest verbs plus a generated increment function
- …and 6 more — see Commits

## Commits

### Features

- *(fanout)* DeliveryDecision::Superseded — a conflated frame is convergent, not lost, and drop% now subtracts it (`f83503a`)
- *(bench)* open-loop --rate, a fixed repetition policy, and randomised run order — the last three harness defects (`035c306`)
- *(bench)* plot every repetition in execution order — a step is the machine changing, which no mean, median or spread can say (`7ec41bc`)
- *(bench)* a per-run delivery budget so every tier moves the same frames (`107f937`)
- *(core)* direct-mode pull cursor — group by xid, hold back a page-cut transaction, persist the xid8 horizon (`78bb873`)
- *(client)* postgrest change source for direct mode — page by transaction, drain to the horizon (`1e53137`)
- *(client)* realtime private-channel doorbell for direct mode, with the documented join/system error split (`ae7a032`)
- *(client)* direct-mode write path — three plain postgrest verbs plus a generated increment function (`15592be`)
- *(cli)* nostos link --mode direct generates the change log, triggers, nostos_pull, nostos_increment, doorbell and RLS (`87b8de3`)
- *(cli)* nostos doctor --mode direct verifies the deployed schema, grants, policies and that pull pages by transaction (`060f624`)
- *(core)* cross-platform conformance suite, plus the retention window as a first-class 410 instead of a short page (`e0175ee`)
- direct-mode push path — scope-stamped token registry, device presence, and a wake trigger that debounces without blocking writers (`c200073`)
- *(direct)* nostos_snapshot — the way back from a 410, and a fifth conformance case for the reap it implies (`049eb94`)

### Bug Fixes

- *(bench)* a timed-out run reports no ops/sec or drop% — the 120s default silently corrupted the 1k tier on a 4-core host (`49eff57`)
- *(bench)* finish on quiescence, not a loss-free target — any shed event made the old wait loop spin to the deadline and dilute ops/sec (`6151a68`)
- *(direct)* four bugs found by running the generated SQL through a real Supabase stack (`1f21c99`)

### Documentation

- *(adr)* ADR-0045 per-key conflating session sinks — built, measured -39.2% at 1k, reverted per the D7 gate (`ba0205b`)
- *(adr)* ADR-0045 attempt 2 — mpsc fast path costs nothing, but drop% counts supersedes as loss so it stays on a branch (`3539b5e`)
- *(adr)* ADR-0045 attempt 3 — drop% now subtracts supersedes (14.18% -> 12.28% on the same run); cross-session ops/sec are not comparable on this host (`d263ffe`)
- *(bench)* first valid Linux figures — 1k tier medians 561,338 ops/sec contended, plus the two harness bugs that host exposed (`f8329af`)
- *(plan)* harness defects 2 and 5 built; a slow host finds bugs a fast one structurally cannot (`9d29d1e`)
- *(bench)* repetition policy, randomised order and open-loop rate become the contract, not a harness detail (`b6bbaa4`)
- *(bench)* refresh the stale main.rs line reference for the bench's own replicator config (`f4ef056`)
- *(bench)* the series plot is part of reading a result, not an extra artifact (`2b394d4`)
- *(bench)* the quiet arm and the 1k-10k budgeted ladder on the Linux host (`7ab4075`)
- *(plan)* the on-device shapes for running nostos without an always-on server (`a279d10`)
- *(plan)* split the embedded shape by who owns the database — per-user postgres needs no nostos-server at all (`2801784`)
- *(plan)* shared-database topology decided — nostos-server is the architecture, and it costs two dollars a month (`5dbacfb`)
- *(plan)* the device-side backend adapter — supabase broadcast is the multiplexer, so no nostos server is needed at all (`95a70b6`)
- *(plan)* the direct-mode sync protocol, with the six rules the prior art publishes as warnings (`871794a`)
- *(plan)* a change log plus the snapshot horizon gives the device transactional consistency with no server (`30fe29e`)
- *(plan)* direct mode needs only a post and a socket, so it reaches every sdk — and the seam moves to nostos-core (`6825dce`)
- *(plan)* fix the sdk table — capacitor is a webview wasm target, react native has no rust crate of its own (`71f3622`)
- *(plan)* atlet is the test bed — a third syncadapter, and its powersync migration is the security argument in sql (`ccae8bf`)
- *(plan)* direct mode is the default only where postgrest+realtime exist, and push needs a credential holder not a daemon (`165e627`)
- *(plan)* research push and presence for direct mode against the apple, firebase, supabase and w3c docs (`0a1dfc1`)

### Testing

- *(router)* convergence lag is the instrument ADR-0045 needed — 0 of 8 keys stale vs 8 of 8 for drop-on-full at the same memory (`d1a545b`)

### Miscellaneous Tasks

- strip the eight make targets pointing at the deleted flutter fixtures (`db34aa6`)

### Other

- *(bench)* conflating session sink per ADR-0045 — correct but 39% below the 1k headline, held back pending an mpsc fast path (`5eef29e`)
- *(bench)* overflow conflation with the mpsc fast path intact — no throughput regression at 1k, blocked on drop% counting supersedes as loss (`aad729e`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
