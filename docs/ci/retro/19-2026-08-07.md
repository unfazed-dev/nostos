# [arxa-builder][arxa-tester] 2026-08-07 — sync rules at runtime: check report, GET/PUT /rules, admin toggle editor

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder · `[arxa-tester]` arxa-tester

**Date:** 2026-08-07 (historical — replayed as retro PR #19; the PR page shows the replay date)

**Branch:** `retro/19-2026-08-07` · **merge:** `6107737` · **commits:** 11

## What is now true

- add nostos rules check validation report
- expose the active ruleset at GET /rules
- add PUT /rules to mutate the active ruleset over HTTP
- gate the rules mutation route behind an operator admin token
- make admin-auth audit test immune to ANSI-colored tracing output
- add sync-rules toggle editor to the admin console
- stop CORS boot panic on non-empty NOSTOS_CORS_ORIGINS (C1)
- allow PUT in explicit-origins CORS methods for admin panel
- atlet demo train/shop/session flows with connectivity LED and guard, uuid client ids, newest-first ordering, plus server write-back coercion and oplog compaction fixes

## Commits

### Features

- add nostos rules check validation report (`8694d9b`)
- expose the active ruleset at GET /rules (`f25b70f`)
- add PUT /rules to mutate the active ruleset over HTTP (`fde4579`)
- gate the rules mutation route behind an operator admin token (`8a6074e`)
- add sync-rules toggle editor to the admin console (`a2242f8`)
- atlet demo train/shop/session flows with connectivity LED and guard, uuid client ids, newest-first ordering, plus server write-back coercion and oplog compaction fixes (`8bdd31c`)

### Bug Fixes

- make admin-auth audit test immune to ANSI-colored tracing output (`6a980d3`)
- stop CORS boot panic on non-empty NOSTOS_CORS_ORIGINS (C1) (`719fd11`)
- allow PUT in explicit-origins CORS methods for admin panel (`97a8bc7`)

### Documentation

- document the three-mode sync-rules suite (`9f33cc4`)

### Testing

- prove sync-rules mode-switch truth transfer (`1f211b4`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
