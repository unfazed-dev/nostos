# [arxa-builder][arxa-designer] 2026-07-30 — paced fake replicator and a docs and SDK fix sweep

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder · `[arxa-designer]` arxa-designer

**Date:** 2026-07-30 (historical — replayed as retro PR #13; the PR page shows the replay date)

**Branch:** `retro/13-2026-07-30` · **merge:** `72bd86b` · **commits:** 23

## What is now true

- infra: pace and bound the fake replicator so the dev server stops firehosing (A10)
- sdk: derive swift harness paths and simulator UDID instead of hardcoding them (A9)
- sdk: capture kotlin logcat once so the verdict cannot race the proof lines (A9)
- sdk: filter swift simulator discovery to iPhone/iPad and make the plans index authoritative
- sdk: expose tauri subscribe command, repair malformed csproj XML, and fix react-native strict typecheck
- correct fabricated github org to unfazed-dev and replace unregistered domain, entity, and contact placeholders
- sdk-e2e: stop pipefail SIGPIPE from failing successful proof-line matches and correct the 7-slice count
- flutter-sdk: correct invented NostosDatabase.supabase params in docs and gate doc snippets on the real signatures
- flutter-sdk: forward rotated auth tokens into the live sync client so sessions survive token expiry
- docs: correct the ADR-0017 addendum — 1.4x not 2.6x required methods, scope the live-only write claim to NostosSocket, and say Err throws

## Commits

### Features

- *(infra)* pace and bound the fake replicator so the dev server stops firehosing (A10) (`08f85c2`)

### Bug Fixes

- *(sdk)* derive swift harness paths and simulator UDID instead of hardcoding them (A9) (`b2f5480`)
- *(sdk)* capture kotlin logcat once so the verdict cannot race the proof lines (A9) (`3eb4b78`)
- *(sdk)* filter swift simulator discovery to iPhone/iPad and make the plans index authoritative (`b80afed`)
- *(sdk)* expose tauri subscribe command, repair malformed csproj XML, and fix react-native strict typecheck (`ce2a373`)
- correct fabricated github org to unfazed-dev and replace unregistered domain, entity, and contact placeholders (`50f87aa`)
- *(sdk-e2e)* stop pipefail SIGPIPE from failing successful proof-line matches and correct the 7-slice count (`1159092`)
- *(flutter-sdk)* correct invented NostosDatabase.supabase params in docs and gate doc snippets on the real signatures (`b76d608`)
- *(flutter-sdk)* forward rotated auth tokens into the live sync client so sessions survive token expiry (`e3736dd`)
- *(docs)* correct the ADR-0017 addendum — 1.4x not 2.6x required methods, scope the live-only write claim to NostosSocket, and say Err throws (`0b8754b`)

### Documentation

- scope the A10 CPU measurement to server-side load and note bench ignores NOSTOS_FAKE_* (`f319ace`)
- record A7 drift fix and correct the sdk-e2e strict-mode slice count (`6908bd9`)
- record A9 closed with 10/10 sdk-e2e slices passing in strict mode (`bf4715c`)
- add plans index classifying all 23 plans and flag the three that misdirect (A8) (`5310ef5`)
- close C15 with per-SDK publishability evidence and correct the launch draft's SDK status (`3edbf58`)
- *(sdk)* correct uniffi-bindgen regeneration commands and cite tauri camelCase arg conversion (`034b1fb`)
- *(sdk)* scope the csproj evaluation claim to hand-declared deps and drop the unverified SIGPIPE asymmetry story (`c6c7e98`)
- adopt ADR-0028 rejecting materialized typed tables and rewrite the flutter redesign plan as an as-built record (`7ea15f4`)
- *(api)* add a per-SDK API reference with source-cited signatures and gate the flutter page on the real factories (`fd6ff68`)
- *(api)* explain why SDK read models and reactivity differ, citing the crate graph and ADR-0017 (`31b80f4`)
- correct six ADR and roadmap status lines that understated shipped work and add the completion audit (`f0f2cdf`)
- *(adr-0017)* reject the IndexedDB mirror and record that the browser SDK is live-only, not merely non-durable (`3efb761`)

### Miscellaneous Tasks

- *(sdk)* package all nine SDKs at 0.1.0 with LICENSE, repository metadata, and READMEs (`d98c62f`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
