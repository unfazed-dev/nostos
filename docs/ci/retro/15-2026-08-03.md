# [arxa-builder][arxa-designer] 2026-08-03 — sign-out wipe and setToken across SDKs, JWT exp on HS256 (ADR-0029)

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder · `[arxa-designer]` arxa-designer

**Date:** 2026-08-03 (historical — replayed as retro PR #15; the PR page shows the replay date)

**Branch:** `retro/15-2026-08-03` · **merge:** `856449d` · **commits:** 6

## What is now true

- flutter-sdk: port signOut local-state wipe via clear_local_state (ADR-0029 D3)
- infra,cloud: enforce JWT exp on HS256 auth path (ADR-0029 §Decision-4)
- swift-sdk: prove setToken + signOut wipe on ios-sim via NostosSmoke (ADR-0029, WS4-D3)
- react-native-sdk: iOS TurboModule over nostos-swift UniFFI; signOut/setToken reach iOS via JSI (ADR-0029, WS4-D3)
- react-native-sdk: iOS TurboModule file-backed wipe + watchChanges push + self-contained podspec + fat sim staticlib (ADR-0029, WS4-D3)

## Commits

### Features

- *(flutter-sdk)* port signOut local-state wipe via clear_local_state (ADR-0029 D3) (`d63dd50`)
- *(infra,cloud)* enforce JWT exp on HS256 auth path (ADR-0029 §Decision-4) (`d65dc87`)
- *(swift-sdk)* prove setToken + signOut wipe on ios-sim via NostosSmoke (ADR-0029, WS4-D3) (`59a2fe8`)
- *(react-native-sdk)* iOS TurboModule over nostos-swift UniFFI; signOut/setToken reach iOS via JSI (ADR-0029, WS4-D3) (`2c4120a`)
- *(react-native-sdk)* iOS TurboModule file-backed wipe + watchChanges push + self-contained podspec + fat sim staticlib (ADR-0029, WS4-D3) (`c2e151e`)

### Documentation

- *(adr-0029)* record Flutter signOut + HS256 exp enforcement status (WS4 audit) (`3fda345`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
