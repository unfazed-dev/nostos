# [arxa-builder][arxa-designer][arxa-tester] 2026-08-31 — shared-secret bearer sync auth

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder · `[arxa-designer]` arxa-designer · `[arxa-tester]` arxa-tester

**Date:** 2026-08-31 (historical — replayed as retro PR #30; the PR page shows the replay date)

**Branch:** `retro/30-2026-08-31` · **merge:** `54b77c8` · **commits:** 3

## What is now true

- server: NOSTOS_SYNC_AUTH=bearer — one shared secret resolves to one fixed principal (ADR-0010 addendum)
- sdk/flutter: REST base keeps the sync URL path prefix — proxied sync keeps /schema and /push-tokens reachable
- server: tenant column resolves under bearer auth — fully-offline doorbell hints regain their tenant (ADR-0010 addendum)

## Commits

### Features

- *(server)* NOSTOS_SYNC_AUTH=bearer — one shared secret resolves to one fixed principal (ADR-0010 addendum) (`760962f`)

### Bug Fixes

- *(sdk/flutter)* REST base keeps the sync URL path prefix — proxied sync keeps /schema and /push-tokens reachable (`ef3a40b`)
- *(server)* tenant column resolves under bearer auth — fully-offline doorbell hints regain their tenant (ADR-0010 addendum) (`0a322af`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
