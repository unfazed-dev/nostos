# [arxa-tester][arxa-builder][arxa-designer] 2026-09-21 — ack-progress scan experiments and Android toolchain bumps

**Pipeline stage / agent:** `[arxa-tester]` arxa-tester · `[arxa-builder]` arxa-builder · `[arxa-designer]` arxa-designer

**Date:** 2026-09-21 (historical — replayed as retro PR #35; the PR page shows the replay date)

**Branch:** `retro/35-2026-09-21` · **merge:** `db7226a` · **commits:** 15

## What is now true

- fanout: default the ack-progress scan to every 16 events — the per-event O(sessions) fold cost 2.23x throughput at 100k sessions, guarded by a lags-but-never-overshoots test
- flutter: move the example android project from example/lib/android to example/android where flutter looks for it
- fanout: revert the ack-progress default to 1 — six interleaved tiers show 7.99x within-arm spread, the 2.23x was run-to-run noise
- bench: enforce the mid-run headroom rule in fanout-100k-diag.sh — sample host CPU every 10s, per-tier valid verdict, stop hardcoding LINUX_DIAG_EXIT=0
- bench: count com.apple.Virt* and kernel_task as harness — Docker on Apple Silicon runs the VM under Virtualization.framework, so the gate killed a good tier at 574% self-contention
- bench: recalibrate the mid-run headroom rule to aggregate non-harness CPU <= 150% on >= 95% of samples — the 20%-per-process bar was 2% of a 10-core host and measured the wrong quantity

## Commits

### Bug Fixes

- *(flutter)* move the example android project from example/lib/android to example/android where flutter looks for it (`6e5c33a`)
- *(bench)* enforce the mid-run headroom rule in fanout-100k-diag.sh — sample host CPU every 10s, per-tier valid verdict, stop hardcoding LINUX_DIAG_EXIT=0 (`fac26c7`)
- *(bench)* count com.apple.Virt* and kernel_task as harness — Docker on Apple Silicon runs the VM under Virtualization.framework, so the gate killed a good tier at 574% self-contention (`369e747`)
- *(bench)* recalibrate the mid-run headroom rule to aggregate non-harness CPU <= 150% on >= 95% of samples — the 20%-per-process bar was 2% of a 10-core host and measured the wrong quantity (`4646c58`)

### Documentation

- naming + domain decision — no domain is needed to launch, io.github closes the maven gate, qairn recommended over nostos (`d146ee7`)

### Performance

- *(fanout)* default the ack-progress scan to every 16 events — the per-event O(sessions) fold cost 2.23x throughput at 100k sessions, guarded by a lags-but-never-overshoots test (`f49d5fd`)
- *(fanout)* revert the ack-progress default to 1 — six interleaved tiers show 7.99x within-arm spread, the 2.23x was run-to-run noise (`78c026d`)

### Miscellaneous Tasks

- *(android)* gradle 9.7.1 + agp 9.4.1 on the kotlin and react-native sdks — agp 9 built-in kotlin reads kotlin.srcDirs only, and verifyAar now fails a build whose aar ships an empty classes.jar (`bd9fcb8`)
- *(rename)* add the nostos->qairn sweep script and its inventory, dry-run by default with the wire/storage/pg identity classes gated behind opt-in flags (`3682034`)

### Other

- *(fanout)* per-stage timers (match/deliver/ack-scan) so the 100k cliff can be read as slow-vs-starved (`8afa440`)
- *(fanout)* cpu-vs-wall readout in the 10k probe, plus the stage ladder recorded as INVALID — shared store mutex is the first suspect for the 100k cliff (`6434f05`)
- *(fanout)* real wire-encode sink in the walk probe, plus the gated 100k ladder that located the cliff and the two reversals it forced (`99200cd`)
- *(fanout)* mark the 2.23x ack-coalescing result provisional — a same-config rerun disagreed 1.8x, replication in flight (`d5b9b30`)
- *(fanout)* enforce the <1% drop rule per tier and record the v2 replication — no tier in either six-tier run met it, so neither measured throughput (`2631c8b`)
- *(fanout)* drop-rate ladder 10k-100k — 50k is the last reproducibly sub-1% rung, the 55k/60k boundary dissolves on replication (`8d0ecc4`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
