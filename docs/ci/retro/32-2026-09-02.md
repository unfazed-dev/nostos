# [arxa-tester][arxa-builder][arxa-designer] 2026-09-02 — security hardening, sequential fan-out, benchmark reruns

**Pipeline stage / agent:** `[arxa-tester]` arxa-tester · `[arxa-builder]` arxa-builder · `[arxa-designer]` arxa-designer

**Date:** 2026-09-02 (historical — replayed as retro PR #32; the PR page shows the replay date)

**Branch:** `retro/32-2026-09-02` · **merge:** `b143144` · **commits:** 72

## What is now true

- infra: clamp client acks to what the sink actually delivered
- client: unpublish bench_apply so it stops breaking fresh-slot e2e tests
- client: bench teardown must not panic or batch its two cleanup statements
- write-back: collapse the cross-tenant rejection into one non-disclosing message
- sync: refuse over-cap snapshots loudly and bound live sessions per principal
- security: require jwt exp, validate nbf, add iss allowlist, bound jwks staleness, enable slot eviction by default
- domain: make predicate evaluation three-valued so Not over an absent column stops over-delivering
- security: throttle failed admin-token attempts and add opt-in websocket origin allowlist
- security: add opt-in metadata protection gating GET /schema on sync auth and GET /rules on the admin token
- cli: nostos pull sends bearer token via --token/NOSTOS_TOKEN when server runs NOSTOS_PROTECT_METADATA=1
- …and 10 more — see Commits

## Commits

### Features

- *(security)* require jwt exp, validate nbf, add iss allowlist, bound jwks staleness, enable slot eviction by default (`ac78ae5`)
- *(security)* throttle failed admin-token attempts and add opt-in websocket origin allowlist (`b8b3d14`)
- *(security)* add opt-in metadata protection gating GET /schema on sync auth and GET /rules on the admin token (`a69014f`)
- *(cli)* nostos pull sends bearer token via --token/NOSTOS_TOKEN when server runs NOSTOS_PROTECT_METADATA=1 (`b56fffe`)
- *(cli)* nostos pull token hardening — flag beats env, redacted Display, refuse cleartext http to non-loopback unless --allow-insecure-token (`4744dce`)
- *(server)* keep 1 GiB NOSTOS_SLOT_MAX_LAG default, warn on 0, add doctor max_slot_wal_keep_size check (ADR-0043) (`be853dd`)
- *(bench)* record fan-out finish time in probe_10k and key ladder pass-2 on drop% < 1 (`de99ec1`)

### Bug Fixes

- *(infra)* clamp client acks to what the sink actually delivered (`c856129`)
- *(client)* unpublish bench_apply so it stops breaking fresh-slot e2e tests (`e27bc2e`)
- *(client)* bench teardown must not panic or batch its two cleanup statements (`acea45f`)
- *(write-back)* collapse the cross-tenant rejection into one non-disclosing message (`c930997`)
- *(sync)* refuse over-cap snapshots loudly and bound live sessions per principal (`4b7386d`)
- *(domain)* make predicate evaluation three-valued so Not over an absent column stops over-delivering (`0abeee0`)
- *(auth)* enforce nbf and issuer allowlist on HS256 path, add tests for both verifiers (`899530e`)
- derive REPO_ROOT from git toplevel in kotlin/rn e2e scripts instead of hardcoded developer_ssd path (`92001dd`)
- set SDKROOT in nostos_flutter build hook so ring compiles under the Xcode script-phase env on macOS (`3254019`)
- treat empty or bare-name SDKROOT as absent in nostos_flutter build hook (`444c432`)
- *(bench)* make probe_10k undelivered breakdown exact per event instead of integer events-per-client (`22aa28a`)

### Documentation

- *(plans)* correct the ack finding, record the stream-snapshot fix and boot guard (`9958c2d`)
- *(plans)* record the pg root cause and what was deliberately left (`b7ea83a`)
- *(plans)* record the re-verified make ci and boot-guard results (`bdcfe13`)
- *(plans)* record the orphaned-slot hazard that faked a fan-out regression (`6416523`)
- *(plans)* record the full pg e2e suite green, retiring the 2-failure caveat (`b8f20ea`)
- *(plans)* plan the close-all-findings pass and correct two audit pointers (`005c1e7`)
- record the fix-all pass outcomes, verification runs, and the two carried-forward exclusions (`bcd2d37`)
- note that the websocket origin allowlist does not cover the iroh transport (`d527d04`)
- record the green full-gate result (`41f2b9c`)
- record stress and pressure results plus the benchmark contention caveat (`2f3d6ca`)
- record the real bench binary names and the unrun 10k soak (`4a5a9fa`)
- record nostos pull --token/NOSTOS_TOKEN follow-up as done in b56fffe (`b828f46`)
- *(cli)* note ps visibility and cleartext caveats for nostos pull --token (`834303f`)
- record code cross-check verdicts and finding-4 closure in 899530e (`45e85b1`)
- *(audit)* record NOSTOS_SLOT_MAX_LAG decision (ADR-0043) as closed (`cdc7079`)
- record 10k soak and throughput re-measure still host-blocked on 4 orphan context-mode procs (`39e889e`)
- *(adr)* note 1 GiB slot-lag threshold is unmeasured, name revisit trigger (`f18607e`)
- ADR-0044 cargo build-dir on external SSD, dep debuginfo off, weekly mtime sweep (`2347780`)
- ADR-0044 sweep prunes whole idle workspace dirs, never single files (`288050c`)
- ADR-0044 record verification of nostos_flutter rust build-dir and IDE gradle daemon on external volume (`f2b9d2a`)
- *(bench)* mark scale ladder as eval-only, not comparable to PowerSync figures (`495af19`)
- plan for shared full-path Nostos vs PowerSync harness (`89e7fcf`)
- record grilled decisions for the PowerSync full-path harness plan (hold, 100/500/1k ladder, MongoDB Step 0) (`6307e49`)
- mark finish-time probe and drop% pass-2 done with the validated 20k/300 run and correct the ladder breakdown figures (`cfecca6`)
- numbers table with host load1 columns and the INVALID first diagnostic attempt (`b5dd15c`)
- run 1 (100k x 500, valid) falsifies kernel TCP pressure and swap; next run is the original 5000-event shape (`6ff274b`)
- record run 1 100k and 50k control results in fan-out cliff diagnosis plan (`ba874d3`)
- run 2 attempt 1 marked INVALID (host contention mid-run); partial numbers kept (`9132089`)
- headroom rule caption (start gate vs mid-run non-harness CPU) and attempt-1 trace marked suggestive (`16bed3b`)
- run 2 attempt 2 INVALID (sustained non-harness CPU); diagnosis blocked on quiet host (`cad8454`)
- one-page plain-English infographic of Nostos and the 2026-09-02 scale tests (`a389e4e`)
- add inline SVG charts to the plain-English one-pager (speed by tier, loss per 1k, before/after fix) (`0fb789f`)
- tighten one-pager — larger type, fewer words (`67eef40`)

### Performance

- fan out sequentially instead of spawning one task per session per event; probe waits for subscribe quorum (`4bba0fb`)
- share one Arc<ReplicationEvent> per fan-out instead of cloning per session; 10k soak on Linux meets <1% drops (`a7bcd91`)

### Styling

- rustfmt the any-collapse test assertion (`7958877`)
- rustfmt the security-hardening pass (`3c2d76a`)
- allow excessive bools on the clap config struct (`dab4360`)

### Testing

- *(infra)* ack fixtures must deliver before acking, not simulate progress (`5135315`)

### Miscellaneous Tasks

- *(flutter)* refresh the glue-crate lockfile to the 0.2.0 workspace versions (`93faf95`)
- *(flutter)* sync atlet pubspec.lock to nostos_flutter 0.2.0 (`a99c43f`)

### Other

- record 2026-09-02 throughput re-measure as host-contended and aborted; baseline unchanged (`cc00d04`)
- record run2 re-measure as host-contended (VS Code Code Helper); baseline unchanged (`ec824e5`)
- re-measure throughput (3 passes) and 10k soak after clearing host contention (`be4c8f0`)
- native 3-pass re-measure on fixed fan-out (median 2,618,601 ops/sec, 0.00% drops) and 10k soak; update headline (`0b7fd3b`)
- probe takes a listeners arg (N loopback addrs), raises nofile to hard limit, reports completed+peak RSS; add scale-ladder.sh (`1dc27b2`)
- scale ladder 20k-100k on Linux container + macOS 10k re-measure and soaks (`c5929b9`)
- make probe subscribe-quorum wait progress-based and report late subscribers; add 30k-100k rerun script (`d67e573`)
- re-run 30k-100k ladder with progress-based quorum (`2608a2a`)
- add 100k fan-out cliff diagnostic (probe progress line + in-container VM sampler) (`498a4fc`)
- gate fan-out diagnostic measurements on host headroom (load1 < 8, no other bench container, lock free) (`07d587b`)
- probe reports whether the fan-out loop returned (replicator budget exhausted) vs still running at window end (`38dd0ea`)
- fan-out diagnostic takes tier specs from argv; inner tier exits 0 after sampler teardown (`9ee9b10`)
- raw logs for the 100k fan-out cliff diagnosis (run 1 valid; run 2 attempts 1-2 INVALID, host-contended) (`d828b6e`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
