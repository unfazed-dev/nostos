# [arxa-builder][arxa-tester] 2026-09-24 — nostos rename groundwork: Docker fix, CI decision record, env/config fallback (ADR-0046), rename and retro tooling, arxa CI/CD adoption

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder · `[arxa-tester]` arxa-tester

**Date:** 2026-09-24 (historical — replayed as retro PR #38; the PR page shows the replay date)

**Branch:** `retro/38-2026-09-24` · **merge:** `3252b6d` · **commits:** 26

## What is now true

- deps: rustls 0.23.45 (RUSTSEC-2026-0285) + rust 1.98 pins in Dockerfile and bench scripts
- docker: CMD not ENTRYPOINT so compose/fly command overrides work, .dockerignore cuts the 9 GB build context
- env and config rename fallback, every NOSTOS_* read routed through nostos_infra::env (ADR-0046)
- scripts: nostos_rename, one rule table for the forward rename and the history rewrite
- scripts: retro rewrite tooling for the nostos history — filter-repo rename, day-grouped retro merges, hash fixups, paced GitHub replay
- scripts: postfix.sh and a two-commit retro PR #39 (style + hash fixups), apply.py tip check, auto tags for the growing day
- scripts: nostos_rename census fails on names held in a migration but renamed elsewhere
- scripts: hold prose naming the nostos db superuser in the rename rules
- scripts: postfix.sh runs flutter pub get as CI does, pubspec.lock and plugin registrant re-sorts in scope

## Commits

### Features

- env and config rename fallback, every NOSTOS_* read routed through nostos_infra::env (ADR-0046) (`d638970`)
- *(scripts)* nostos_rename, one rule table for the forward rename and the history rewrite (`1ab68f9`)
- *(scripts)* retro rewrite tooling for the nostos history — filter-repo rename, day-grouped retro merges, hash fixups, paced GitHub replay (`697684b`)
- *(scripts)* postfix.sh and a two-commit retro PR #39 (style + hash fixups), apply.py tip check, auto tags for the growing day (`b9d1269`)
- *(scripts)* nostos_rename census fails on names held in a migration but renamed elsewhere (`3f14a2f`)
- *(scripts)* postfix.sh runs flutter pub get as CI does, pubspec.lock and plugin registrant re-sorts in scope (`790b83e`)

### Bug Fixes

- *(deps)* rustls 0.23.45 (RUSTSEC-2026-0285) + rust 1.98 pins in Dockerfile and bench scripts (`37ab8dd`)
- *(docker)* CMD not ENTRYPOINT so compose/fly command overrides work, .dockerignore cuts the 9 GB build context (`79abc2d`)
- *(scripts)* hold prose naming the nostos db superuser in the rename rules (`539e88a`)

### Documentation

- *(ci)* arxa-cicd decision record and nostos rename research, name map (`808b867`)
- *(adr)* 0046 pre-rename env, config and binary names keep working until 1.0 (`1c94517`)
- *(scripts)* nostos_rename post-rename steps, uniffi C# regen and npm lock order (`7a1f040`)
- *(scripts)* retro rewrite runbook and draft retro PR titles (`325bc8c`)
- *(ci)* decision 3b counts after the dry run, titles path, post-rename PR #39 (`96f538f`)
- *(scripts)* retro runbook, build-dir cleanup and the target link make ci needs under CARGO_TARGET_DIR (`0947145`)
- *(ci)* PR template and tag map SSOT, setup.md, explainer.html, worktree verbs in CLAUDE.md (`f8d4fd6`)
- *(ci)* decision 0 job count (`28b27f0`)

### Styling

- *(atlet)* dart format (`8eeb2ba`)

### Testing

- *(scripts)* synthetic-repo invariants for the retro rewrite (`7b6c84c`)

### Miscellaneous Tasks

- gitignore .nostos/ and every supabase/.temp/ (`e44c643`)
- gitignore .worktrees/ (`5d0e94b`)
- pre-rename binary symlinks in Docker image, release tarballs and Homebrew (ADR-0046) (`9ea9806`)
- *(scripts)* drop rename-to-qairn.sh, superseded by scripts/nostos_rename (`b267100`)
- worktree verbs, pre-push main guard and WorktreeCreate hook (`03cc9d7`)
- scripts/check.sh one root check, commit-subject gate, warn-first PR-title check, read-only token (`8d8c157`)
- keep the pre-rename .nostos/ dir ignored after the rename (`1c455a6`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
