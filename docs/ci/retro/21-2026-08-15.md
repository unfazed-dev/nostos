# [arxa-builder] 2026-08-15 — push notifications: presence index, token store, FCM/APNs/Web Push rails, router

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder

**Date:** 2026-08-15 (historical — replayed as retro PR #21; the PR page shows the replay date)

**Branch:** `retro/21-2026-08-15` · **merge:** `513509d` · **commits:** 22

## What is now true

- add session presence index, PushNotifier port, typed column extraction
- add PgTokenStore with cairn_push_tokens schema
- add SyncClient disconnect/resume with UniFFI exposure
- add off-hot-loop push enqueue with bench toggle and drop counters
- add FCM, APNs, and Web Push provider rails
- add push router with coalescing, offline expansion, and templates
- add push-token REST routes and server push wiring
- add flutter and node push token registration
- add push token registration to kotlin, swift, and dotnet SDKs
- add react-native push registration and wake bridge
- …and 4 more — see Commits

## Commits

### Features

- add session presence index, PushNotifier port, typed column extraction (`32e5426`)
- add PgTokenStore with cairn_push_tokens schema (`80dab4f`)
- add SyncClient disconnect/resume with UniFFI exposure (`cec078b`)
- add off-hot-loop push enqueue with bench toggle and drop counters (`aad9c56`)
- add FCM, APNs, and Web Push provider rails (`1041cbc`)
- add push router with coalescing, offline expansion, and templates (`ad8401d`)
- add push-token REST routes and server push wiring (`b8d1437`)
- add flutter and node push token registration (`d00205d`)
- add push token registration to kotlin, swift, and dotnet SDKs (`8ee755c`)
- add react-native push registration and wake bridge (`1821756`)
- add experimental web push with storage seam and service worker (`e579f74`)
- extend capacitor plugin with beta push registration (`540c1ff`)
- add experimental live activities update rail (`3ae7140`)

### Bug Fixes

- harden push security (path encoding, cross-tenant token guard, SSRF, caps) (`b24e16f`)

### Documentation

- record kimitail audit findings (38 findings, net -1209 lines) (`06e8c77`)
- add ADR-0037 sync-aware push notifications with research and plan (`0b877b9`)
- tick push plan 1.1-1.2/1.4/5.1, close ADR-0037 extraction hole (`a6246f8`)
- tick push plan 1.3/2.1-2.3, amend ADR-0037 offline doorbell (`b99736d`)
- tick push plan 2.4, 3.1-3.3, 4.1-4.2 (`36c904d`)
- tick push plan 5.2-5.4 and 6.1-6.4 (`8d6ff3e`)

### Testing

- pin SDK attachment wire contract to nostos-core in CI (`3c08f9a`)

### Miscellaneous Tasks

- lockfile for push router and REST route dependencies (`5f4fa8b`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
