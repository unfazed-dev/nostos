# [arxa-builder] 2026-08-06 — atlet comparison suite: SyncAdapter contract, schema, Flutter pilot

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder

**Date:** 2026-08-06 (historical — replayed as retro PR #18; the PR page shows the replay date)

**Branch:** `retro/18-2026-08-06` · **merge:** `da8613b` · **commits:** 55

## What is now true

- scaffold apps/atlet comparison suite with isolation and numbers policy
- freeze Atlet design assets into apps/atlet/design with provenance
- freeze SyncAdapter contract and Core-4+storage metric definitions
- add Atlet canonical schema, RLS, powersync replication plumbing, seed, and per-SDK users script
- move powersync_role password out of migration into executor-agnostic provisioning script
- add local-profile docker compose for nostos-server and self-hosted powersync
- scaffold Atlet flutter pilot with supabase auth and design tokens
- define SyncAdapter contract with externally-observable mark derivation and conformance tests
- add bench clock offset estimation, neutral JSONL store, and core-4 runner math
- guard queueDrain against pre-reconnect acks, make percentile return type explicit
- …and 32 more — see Commits

## Commits

### Features

- scaffold apps/atlet comparison suite with isolation and numbers policy (`0a457a8`)
- freeze Atlet design assets into apps/atlet/design with provenance (`908a845`)
- freeze SyncAdapter contract and Core-4+storage metric definitions (`abc6a3c`)
- add Atlet canonical schema, RLS, powersync replication plumbing, seed, and per-SDK users script (`197e986`)
- add local-profile docker compose for nostos-server and self-hosted powersync (`16cde42`)
- scaffold Atlet flutter pilot with supabase auth and design tokens (`fd901f2`)
- define SyncAdapter contract with externally-observable mark derivation and conformance tests (`de4250a`)
- add bench clock offset estimation, neutral JSONL store, and core-4 runner math (`86e763d`)
- carry flat JWT claims on Principal for rules scoping (`70b18ef`)
- implement nostos adapter for atlet pilot (`e44bb1b`)
- add claims-scoped rules grammar to nostos-domain (`1d9adac`)
- implement powersync adapter for atlet pilot (`e4f3e16`)
- add versioned sync-rules model with canonical checksum (`93484d6`)
- add runtime engine toggle with full wipe between engines (`4ceeaae`)
- compose slot epoch and rules checksum into one sync epoch (`9880232`)
- add training home and detail surfaces over adapter watch streams (`bd045ea`)
- compile sync rules into a per-session decision engine (`448d5b7`)
- read and write nostos_rules.toml (`dca0160`)
- add shop surface backed by bulk read-only products sync (`647431d`)
- estimate replicated table sizes from pg_class reltuples (`64c6b52`)
- wire core-4 plus storage bench runs through the adapter marks (`72707b0`)
- load nostos_rules.toml at server boot (`33bd595`)
- enforce sync rules at subscribe time (`5dc95fc`)
- add analytics tab with internal-eval labeling and postgrest run upload (`2f6eb54`)
- add rules_checksum to the subscribe and resume_info frames (`27cf352`)
- persist and resend the rules checksum from the client (`e951440`)
- *(atlet)* add bottom-nav shell wiring Home/Shop/Analytics (I-1) (`2ae0cbd`)
- warn at startup when sync_mode is all (`e1448d0`)
- hot-reload sync rules and re-scope live sessions (`6dfeba6`)
- add nostos rules init to generate nostos_rules.toml (`2bfa1e2`)
- add nostos rules edit toggle editor (`686f3d3`)
- hint at nostos rules init --force when re-entering toggles mode (`f376fcf`)

### Bug Fixes

- move powersync_role password out of migration into executor-agnostic provisioning script (`25de8b1`)
- guard queueDrain against pre-reconnect acks, make percentile return type explicit (`17f3fcc`)
- correct propagation_ms clock-offset sign (was true_delay - 2x offset) (`b7cdd7a`)
- lift extra JWT claims on the JWKS verification path (`d97bef3`)
- attach nostos adapter connection-state listener before subscribeTables (`4a108b4`)
- remove currentStatus backstop that overwrote correct connected signal (`10e396a`)
- normalize case-insensitive TRUE/FALSE keywords in scope tokenizer (`557c2ce`)
- reject control characters in sync-rules table names (`6158d15`)
- route FAB/spinner color through design token, correct detail snackbar copy, and fix StreamBuilder pop race (`6a7e7ef`)
- reframe Complete as log-and-clear per team-lead UX ruling (`0e79591`)

### Documentation

- retire cross-stage 208x/417x moat multiples for honest same-units framing (`61719dd`)
- annotate atlet design provenance with source-state ruling (`ba232d0`)
- add sync-streams suite implementation plan with operator-ratified rulings (`e000621`)
- record ratified NOSTOS_ADMIN_TOKEN shape in sync-streams Task 21 (`566c313`)
- commit sync-scoping and powersync research docs cited by the stream-suite plan (`5d932da`)
- add ADR-0031 for three-mode sync rules and checksum resync (`52646f8`)
- align ADR-0031 reload and checksum wire-field with D2/D3 rulings (`78c79d8`)
- *(scope)* note Claim always resolves to text on the type itself (`04850f7`)
- record flutter pilot conformance results and freeze adapter spec v1 (`5502f62`)
- drop unimplemented updateSession from adapter spec, bump v1.1 (`9afd3dc`)
- *(atlet)* document db_bytes measurement scope + REPLICA IDENTITY caveat (`ec7c1be`)

### Testing

- verify shop product asset actually resolves via rootBundle (`ad0cc75`)
- cover fresh-ruleset mid-session subscribe and watch_rules notify semantics (`c8c74b1`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
