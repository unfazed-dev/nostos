# [arxa-builder][arxa-designer][arxa-tester] 2026-06-28 — tiers 0–7 foundation: LSN resume, client SDK core, WASM bridge, predicate engine

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder · `[arxa-designer]` arxa-designer · `[arxa-tester]` arxa-tester

**Date:** 2026-06-28 (historical — replayed as retro PR #2; the PR page shows the replay date)

**Branch:** `retro/02-2026-06-28` · **merge:** `4b0302a` · **commits:** 10

## What is now true

- Tier 0+1 foundation — ack-driven LSN resume, /sync auth, server-enforced predicates
- Tier 2 — client SDK core + durable checkpoint + WAL-bloat protection (ADR-0016)
- Tier 2.5 — WASM FFI bridge, the first FFI target (ADR-0015)
- Tier 3 — ADR-0012 predicate engine, slice 1 (boolean tree)
- Tier 3.5 — ADR-0012 slice 2 (typed comparison + JSON extractor)
- Tier 6 — native reactive_scroll example (make the moat visible)
- Tier 7 — safe-SQL-subset predicate compiler (the final verifiable increment)

## Commits

### Features

- Tier 0+1 foundation — ack-driven LSN resume, /sync auth, server-enforced predicates (`48d6875`)
- Tier 2 — client SDK core + durable checkpoint + WAL-bloat protection (ADR-0016) (`804919c`)
- Tier 2.5 — WASM FFI bridge, the first FFI target (ADR-0015) (`3874298`)
- Tier 3 — ADR-0012 predicate engine, slice 1 (boolean tree) (`d94eadb`)
- Tier 3.5 — ADR-0012 slice 2 (typed comparison + JSON extractor) (`066177c`)
- Tier 6 — native reactive_scroll example (make the moat visible) (`2dc32a4`)
- Tier 7 — safe-SQL-subset predicate compiler (the final verifiable increment) (`d0c7fe2`)

### Documentation

- Tier 5 — index experiment built, measured 4-8x regression, REVERTED (`4937378`)

### Testing

- Tier 4 — 10k-predicate fan_out baseline (measure-before-optimize) (`bdbba86`)
- Tier 4.5 — re-measure with parse-once extractor (corrected baseline) (`3162550`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
