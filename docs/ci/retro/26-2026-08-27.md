# [arxa-builder] 2026-08-27 — production SDK surface: Tauri bindings, push retries, API keys, NostosDatabase.local

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder

**Date:** 2026-08-27 (historical — replayed as retro PR #26; the PR page shows the replay date)

**Branch:** `retro/26-2026-08-27` · **merge:** `51f5ce0` · **commits:** 10

## What is now true

- nostos-tauri: production SDK surface — typed JS guest bindings, plugin config, push-token registration parity
- nostos-push: transient rail retry with re-debounce and terminal-only receipts
- nostos-push: hashed-at-rest API key store with per-tenant rate limits, Retry-After on 429s, key CRUD in nostos push CLI
- nostos_flutter: NostosDatabase.local — the no-server entry point

## Commits

### Features

- *(nostos-tauri)* production SDK surface — typed JS guest bindings, plugin config, push-token registration parity (`32d1a34`)
- *(nostos-push)* transient rail retry with re-debounce and terminal-only receipts (`644f681`)
- *(nostos-push)* hashed-at-rest API key store with per-tenant rate limits, Retry-After on 429s, key CRUD in nostos push CLI (`8a9be1e`)
- *(nostos_flutter)* NostosDatabase.local — the no-server entry point (`0b3ff54`)

### Documentation

- add plan for sdk_tauri and sdk_flutter fixes plus nostos-push productionization for arxa integration (`ea29c09`)
- *(nostos-push)* document known Web Push limits (killed-tab no-data-wake, app-owned resubscribe) (`66659ed`)
- *(nostos_flutter)* versioning and tag-truth sections, atlet reference wiring with CI coverage (`580796e`)
- *(adr)* ADR-0041 transport abstraction — ws|iroh dial layer, transport-only scope guard (`a2f61bb`)

### Testing

- *(nostos-tauri)* conformance suite ported from adapter contract v1.1 + ACL lockstep pin, tauri slice wired into CI (`88ca3c6`)

### Miscellaneous Tasks

- *(flutter)* bump nostos_flutter to 0.2.0-dev.1 and record post-0.1.0 drift in CHANGELOG (`8e80d74`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
