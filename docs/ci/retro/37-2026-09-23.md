# [arxa-builder][arxa-tester][arxa-designer] 2026-09-23 — direct-mode engine and push: routing keys, tokens, visible pushes

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder · `[arxa-tester]` arxa-tester · `[arxa-designer]` arxa-designer

**Date:** 2026-09-23 (historical — replayed as retro PR #37; the PR page shows the replay date)

**Branch:** `retro/37-2026-09-23` · **merge:** `5070fe4` · **commits:** 16

## What is now true

- cli: postgres URLs accept every libpq sslmode, and doctor checks nostos_pull by proretset
- push: routing keys a notification tap resolves to, carried on APNs, FCM and Web Push and validated at the edge
- flutter: Nostos.connectDirect — direct-mode engine over PostgREST and Realtime, same outbox and watch streams
- atlet: migrations reproduce the live schema — cart and orders backfilled, atlet_pub publication, nostosDirect engine check
- atlet: direct-mode only — History tab with order detail and push route, bench moved off it, comparison engine removed
- flutter: direct-mode push tokens — registerPushToken/deregister go through the nostos_*_push_token PostgREST RPCs, setToken rotates the push credential
- atlet: push pilot on direct mode — accepts the {nostos: ring} doorbell, background wake cold-opens nostos_direct.sqlite under the user's scope
- core: snapshot rows apply at lsn 0 so later log updates to them are not gated out
- push: direct-mode visible pushes — nostos.push_templates rows ring templated tables as alert/action pushes that reach a quit app
- cli: nostos link --visible writes push_templates in the NOSTOS_PUSH_TABLES grammar, --deploy rolls out direct-mode push through the supabase CLI
- …and 1 more — see Commits

## Commits

### Features

- *(push)* routing keys a notification tap resolves to, carried on APNs, FCM and Web Push and validated at the edge (`fed2087`)
- *(flutter)* Nostos.connectDirect — direct-mode engine over PostgREST and Realtime, same outbox and watch streams (`0e25856`)
- *(atlet)* direct-mode only — History tab with order detail and push route, bench moved off it, comparison engine removed (`e39329a`)
- *(flutter)* direct-mode push tokens — registerPushToken/deregister go through the nostos_*_push_token PostgREST RPCs, setToken rotates the push credential (`b695559`)
- *(atlet)* push pilot on direct mode — accepts the {nostos: ring} doorbell, background wake cold-opens nostos_direct.sqlite under the user's scope (`cf7ca7f`)
- *(push)* direct-mode visible pushes — nostos.push_templates rows ring templated tables as alert/action pushes that reach a quit app (`80fbfec`)
- *(cli)* nostos link --visible writes push_templates in the NOSTOS_PUSH_TABLES grammar, --deploy rolls out direct-mode push through the supabase CLI (`50db45f`)

### Bug Fixes

- *(cli)* postgres URLs accept every libpq sslmode, and doctor checks nostos_pull by proretset (`b7a6b9a`)
- *(atlet)* migrations reproduce the live schema — cart and orders backfilled, atlet_pub publication, nostosDirect engine check (`c536e5c`)
- *(core)* snapshot rows apply at lsn 0 so later log updates to them are not gated out (`30bc87e`)
- *(atlet)* with the push pilot on a phone the server push is the only order banner — no heartbeat means it reaches an open app too (`49e034d`)

### Documentation

- *(adr)* accept ADR-0045 per-key conflating session sinks (`f367aef`)
- Nostos + Flutter, explained like you're 15 (`105de1c`)
- nostos-explained gains the push lane — close an app, toggle push, watch the silent ring wake it (`41fd7d7`)

### Testing

- *(infra)* pg e2e reads every frame through frame_events, so a batched array no longer reads as a missing row (`4236a22`)

### Miscellaneous Tasks

- remove PowerSync from the repo — comparison harness, smoke test, bench ceiling, and every doc, ADR and plan reference (`3a62132`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
