# [arxa-builder] 2026-08-05 — OR-set loud-fail, faulted delivery counter, launch-gap closure

**Pipeline stage / agent:** `[arxa-builder]` arxa-builder

**Date:** 2026-08-05 (historical — replayed as retro PR #17; the PR page shows the replay date)

**Branch:** `retro/17-2026-08-05` · **merge:** `ccecee5` · **commits:** 7

## What is now true

- infra: bump slot_epoch at recreate so reconnect-resume gate fires (ADR-0025 slice 4)
- OR-set loud-fail + NOSTOS_OR_SET_COLUMNS, faulted delivery counter, 10k ack-progress coalesce, CI scale/flutter/sdk-typecheck gates
- sdks: dotnet signOut/setToken binding, RN-Android watchChanges, restore Flutter example + facade tests, kotlin NDK ranlib
- close remaining launch gaps — flutter sdk-e2e (10/10), dotnet CI gate, real-PG slot-exhaustion fix, web/doc sync

## Commits

### Features

- OR-set loud-fail + NOSTOS_OR_SET_COLUMNS, faulted delivery counter, 10k ack-progress coalesce, CI scale/flutter/sdk-typecheck gates (`5229d72`)
- *(sdks)* dotnet signOut/setToken binding, RN-Android watchChanges, restore Flutter example + facade tests, kotlin NDK ranlib (`e749c27`)
- close remaining launch gaps — flutter sdk-e2e (10/10), dotnet CI gate, real-PG slot-exhaustion fix, web/doc sync (`967ed91`)

### Bug Fixes

- *(infra)* bump slot_epoch at recreate so reconnect-resume gate fires (ADR-0025 slice 4) (`7e03608`)

### Documentation

- add remaining-work-2026-08-05 report; correct ADR-0029 D4 live-socket close + audit drift (`1b61733`)
- *(adr-0029)* ratify full-wipe as v1 cross-principal isolation; defer per-principal outbox (inert as specified, weakens isolation if made meaningful) (`18b2b87`)

### Testing

- *(infra)* real-PG write-amp harness (ADR-0025 slice 6) + live-Supabase JWKS cold-stranger probe (`0a52b79`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
