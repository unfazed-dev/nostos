# v0.2.0 (historical — no binaries)

Historical — no binaries. A notes-only release recreated for the tag created on 2026-09-21; `release.yml` never ran for it, and it is not re-run for old code.

**Range:** `v0.1.0`…`v0.2.0` · **retro PRs:** #5–#34 · **commits:** 537

## Tag message

> nostos v0.2.0

## Commits

### Features

- engine readback API (rows_for) for in-memory state enumeration (`31e014d`)
- enforce tenant scoping on write-back path (ADR-0018) (`39a8590`)
- verify Supabase asymmetric JWTs via JWKS with HS256 legacy fallback (ADR-0010 addendum) (`98ce599`)
- nostos CLI with init, dev, doctor, and deploy commands (`117e320`)
- nostos_flutter SDK with prebuilt-binary packaging and bounded client flush (ADR-0015/0016 addendums) (`d1d73ae`)
- todo fixture Nostos live mode with tenant-isolation proof and 5-minute quickstart (`e2a6656`)
- nostos doctor IPv6-only hint, init --bind flag, corrected dev snippet params (`c46b10c`)
- typed payload mapping in PgReplicator, OID-keyed (ADR-0019) (`b630726`)
- PowerSync parity core (SQL-watch P1, dead-letter P2, PATCH writes P3) (`016b352`)
- Flutter SDK parity (SQL-watch FFI, PATCH bridge, watchQuery throttle) (`caf1dfc`)
- Node SDK scaffold (napi-rs), first multi-platform parity proof (`24be1ba`)
- IPv6 egress fix, userspace WARP tunnel script + launch-docs sweep (`b43bc5e`)
- Web JS SDK scaffold (@nostos-sync/web), node-verified apply engine — 5/10 (`ff8b699`)
- Tauri plugin scaffold (nostos-tauri), compiles + clippy clean + 2 integration tests — 6/10 (`14e61b3`)
- Swift SDK scaffold (UniFFI), compiles + iOS cross-compile + bindings generate + typecheck + 2 tests — 7/10 (`b437bb0`)
- Swift SDK iPhone 17 sim E2E harness — connect+query proven on device (`7eb024e`)
- Kotlin/Android SDK scaffold (UniFFI), Tier 1 + .so loads on emulator-5554 — 8/10 (`32f5039`)
- add no-docker E2E server spine for SDK live-replication tests (`d82e565`)
- swift SDK live-replication E2E on iPhone sim (subscribe + query-poll) (`121a8d8`)
- tauri SDK live-replication E2E vs spine (subscribe run-loop) (`6bb1897`)
- kotlin SDK live-replication E2E on API-34 emu (subscribe + re-push poll) (`f4f38ad`)
- web SDK live-replication E2E in headless browser (web-sys WS + idle-flush fix) (`609cf05`)
- make sdk-e2e runner — all 7 SDKs live-replication-E2E (PUSH+ECHO) (`1c027ae`)
- Capacitor SDK (web-only plugin) + live PUSH+ECHO E2E vs spine - 8/10 platforms (`1d63046`)
- .NET SDK scaffold (UniFFI-CS over nostos-client) + cross-compile iOS/Android verify - 9/10 platforms (`261de97`)
- RN SDK TS facade + Codegen TurboModule spec + offline Jest smoke (Wave A) (`90ffb2a`)
- RN SDK Android TurboModule + live emu E2E (Wave B) - 10/10 platforms (`77d6fb9`)
- sdk-e2e runner to 10 SDK slices + ADR-0020 (RN TurboModule) + parity docs to 10/10 (`b1f98c5`)
- dotnet SDK C# live-E2E smoke (PUSH+ECHO via UniFFI-CS) + parity docs to 10/10 (`a131f3b`)
- Flutter offline-first Tasks demo app + per-SDK runner tables + Supabase cloud wiring (`b897b76`)
- wire signed-license verification into nostos-server (nostos-license crate, ADR-0006) (`0b42d03`)
- snapshot-on-subscribe adapter (ADR-0014) (`e92fc8d`)
- enhance settings.json with additional hooks and permissions (`ae5ab85`)
- nostos-server GET /schema endpoint + SQLite-affinity helper (WS1, ADR-0021) (`c08ba0e`)
- WS2 typed storage — read views over cairn_data + instant-local writes (`65678e9`)
- WS3-5 NostosDatabase Dart API + apply_schema _pk (schema-fetch, supabase, demo) (`8757dd1`)
- *(scripts)* add render-playbook.py mirror + make playbook target (`457c9c3`)
- add dashboard shell with responsive navigation and connection handling (`f2e38c6`)
- *(sdk)* Dart reactive facade (Collection<T> + Stream watch + SyncStatus) + example/platform restructure (ADR-0024) (`1a0ddb8`)
- *(example)* finish ADR-0024 adoption — typed codegen writes + SyncStatus (`1275223`)
- *(sync)* persisted op-log writer at fan-out chokepoint (ADR-0025 slice 2) (`1f1544b`)
- *(sync)* slot epoch counter + /metrics gauge (ADR-0025 slice 3) (`5aba1a7`)
- *(sync)* client per-row LSN gating (ADR-0025 slice 4a) (`d6d3b67`)
- *(sync)* op-log compaction + retention (ADR-0025 slice 5) (`4723547`)
- *(sync)* op-log replay-on-reconnect (ADR-0025 slice 4b) (`0de368c`)
- *(sync)* client-side slot-epoch persistence via resume_info frame (ADR-0025 F2) (`4ff2989`)
- *(sync)* graceful oplog writer shutdown drains in-flight batch (ADR-0025 #1) (`c84f948`)
- *(client)* surface write outcomes in SyncStatus; dead-letter only errors (ADR-0027) (`af23b28`)
- *(infra)* pace and bound the fake replicator so the dev server stops firehosing (A10) (`08f85c2`)
- *(web)* WS1 thin slice — validate dual-entry (option c); pkg-web wasm boots in a Worker + ping round-trip (Playwright green) (`66823af`)
- *(kotlin-sdk)* port watch() reactive push to a UniFFI sync callback interface (`ad7e525`)
- *(core)* add clear() to Storage+Outbox for sign-out wipe (ADR-0029), checkpoint->0 atomic (`3b12985`)
- *(web)* WS1 slice 2 — live WS path in Worker, outbox-async writes (browser_live + 38 tests green) (`f338188`)
- *(dotnet-sdk)* port reactive SnapshotSink push facade from kotlin (`44c92e5`)
- *(node-sdk)* port reactive watch() via napi ThreadsafeFunction + SnapshotEmitter seam (`1eeacf8`)
- *(core)* add server-authoritative Increment write op for counters (ADR-0030 Decision 1) (`e9e0972`)
- *(domain)* add HLC + add-wins OR-set merge algebra (ADR-0030, piece 2 slice 1) (`c1818bf`)
- *(core)* OR-set element-merge in storage apply path (ADR-0030, piece 2 slice 2) (`c6b00c7`)
- *(client)* client HLC + or_set_add/remove for optimistic OR-set edits (ADR-0030, piece 2 slice 4) (`9aca794`)
- *(infra)* merge OR-set writes element-wise in PgWriteBack (ADR-0030 slice 3) (`41eadff`)
- *(swift-sdk)* port watch() reactive push via UniFFI SnapshotSink + Swift AsyncStream (ADR-0024) (`b916874`)
- *(react-native-sdk)* port watch() reactive push via Codegen TurboModule callback (ADR-0024) (`fd1ce59`)
- *(tauri-sdk)* port watch() reactive push via tauri ipc::Channel (ADR-0024) (`8d80247`)
- *(web)* port watch() reactive push via wasm-bindgen onChange closure over the on_message pump (ADR-0024) (`7b6b35e`)
- *(capacitor-sdk)* port watch() reactive push via per-table listener callback (ADR-0024) (`793b801`)
- *(client)* add clear_local_state sign-out wipe (ADR-0029 D1, WS4-D3 step 1) (`aae4fe8`)
- *(ffi-wasm)* add NostosEngine::clear + NostosSocket::clearLocalState for sign-out wipe (ADR-0029 D1, WS4-D3) (`ac452fa`)
- *(tauri-sdk)* port signOut + setToken for local wipe (ADR-0029, WS4-D3) (`312ab82`)
- *(dotnet-sdk)* port signOut + setToken for local wipe (ADR-0029, WS4-D3) (`231a8e9`)
- *(node-sdk)* port signOut + setToken for local wipe (ADR-0029, WS4-D3) (`1dec1a0`)
- *(kotlin-sdk)* port signOut + setToken for local wipe (ADR-0029, WS4-D3) (`fed1015`)
- *(swift-sdk)* port signOut + setToken for local wipe (ADR-0029, WS4-D3) (`2d8d621`)
- *(web)* port signOut + setToken via Worker clear->close (ADR-0029, WS4-D3) (`77160cf`)
- *(capacitor-sdk)* port signOut + setToken via clearLocalState (ADR-0029, WS4-D3) (`423fc22`)
- *(react-native-sdk)* port signOut + setToken (TS + Android; iOS TurboModule separate) (ADR-0029, WS4-D3) (`5956c84`)
- *(flutter-sdk)* port signOut local-state wipe via clear_local_state (ADR-0029 D3) (`d63dd50`)
- *(infra,cloud)* enforce JWT exp on HS256 auth path (ADR-0029 §Decision-4) (`d65dc87`)
- *(swift-sdk)* prove setToken + signOut wipe on ios-sim via NostosSmoke (ADR-0029, WS4-D3) (`59a2fe8`)
- *(react-native-sdk)* iOS TurboModule over nostos-swift UniFFI; signOut/setToken reach iOS via JSI (ADR-0029, WS4-D3) (`2c4120a`)
- *(react-native-sdk)* iOS TurboModule file-backed wipe + watchChanges push + self-contained podspec + fat sim staticlib (ADR-0029, WS4-D3) (`c2e151e`)
- *(infra)* close live /sync websocket at jwt exp (ADR-0029 D4) (`969ec36`)
- OR-set loud-fail + NOSTOS_OR_SET_COLUMNS, faulted delivery counter, 10k ack-progress coalesce, CI scale/flutter/sdk-typecheck gates (`5229d72`)
- *(sdks)* dotnet signOut/setToken binding, RN-Android watchChanges, restore Flutter example + facade tests, kotlin NDK ranlib (`e749c27`)
- close remaining launch gaps — flutter sdk-e2e (10/10), dotnet CI gate, real-PG slot-exhaustion fix, web/doc sync (`967ed91`)
- scaffold apps/atlet comparison suite with isolation and numbers policy (`0a457a8`)
- freeze Atlet design assets into apps/atlet/design with provenance (`908a845`)
- freeze SyncAdapter contract and Core-4+storage metric definitions (`abc6a3c`)
- add Atlet canonical schema, RLS, powersync replication plumbing, seed, and per-SDK users script (`197e986`)
- add local-profile docker compose for nostos-server and self-hosted powersync (`16cde42`)
- scaffold Atlet flutter pilot with supabase auth and design tokens (`fd901f2`)
- define SyncAdapter contract with externally-observable mark derivation and conformance tests (`de4250a`)
- add bench clock offset estimation, neutral JSONL store, and core-4 runner math (`86e763d`)
- carry flat JWT claims on Principal for rules scoping (`70b18ef`)
- implement nostos adapter for atlet pilot (`e44bb1b`)
- add claims-scoped rules grammar to nostos-domain (`1d9adac`)
- implement powersync adapter for atlet pilot (`e4f3e16`)
- add versioned sync-rules model with canonical checksum (`93484d6`)
- add runtime engine toggle with full wipe between engines (`4ceeaae`)
- compose slot epoch and rules checksum into one sync epoch (`9880232`)
- add training home and detail surfaces over adapter watch streams (`bd045ea`)
- compile sync rules into a per-session decision engine (`448d5b7`)
- read and write nostos_rules.toml (`dca0160`)
- add shop surface backed by bulk read-only products sync (`647431d`)
- estimate replicated table sizes from pg_class reltuples (`64c6b52`)
- wire core-4 plus storage bench runs through the adapter marks (`72707b0`)
- load nostos_rules.toml at server boot (`33bd595`)
- enforce sync rules at subscribe time (`5dc95fc`)
- add analytics tab with internal-eval labeling and postgrest run upload (`2f6eb54`)
- add rules_checksum to the subscribe and resume_info frames (`27cf352`)
- persist and resend the rules checksum from the client (`e951440`)
- *(atlet)* add bottom-nav shell wiring Home/Shop/Analytics (I-1) (`2ae0cbd`)
- warn at startup when sync_mode is all (`e1448d0`)
- hot-reload sync rules and re-scope live sessions (`6dfeba6`)
- add nostos rules init to generate nostos_rules.toml (`2bfa1e2`)
- add nostos rules edit toggle editor (`686f3d3`)
- hint at nostos rules init --force when re-entering toggles mode (`f376fcf`)
- add nostos rules check validation report (`8694d9b`)
- expose the active ruleset at GET /rules (`f25b70f`)
- add PUT /rules to mutate the active ruleset over HTTP (`fde4579`)
- gate the rules mutation route behind an operator admin token (`8a6074e`)
- add sync-rules toggle editor to the admin console (`a2242f8`)
- atlet demo train/shop/session flows with connectivity LED and guard, uuid client ids, newest-first ordering, plus server write-back coercion and oplog compaction fixes (`8bdd31c`)
- unified API Wave 1 — structured predicates, typed reads, writeBatch, deadLetters, atlet de-SQL (`5cc0e54`)
- close Wave-1 follow-on gaps — atomic writeBatch, dead-letter error/timestamp, OR-set handles, doc-sig script (`4540599`)
- add PN-Counter CRDT tier mirroring OR-set (ADR-0030 addendum) with per-replica max merge across domain/storage/client/FFI/Dart/server (`e9c4645`)
- merge unified-API Wave 1 — Flutter typed surface, atomic writeBatch, dead-letter error/timestamp, OR-set + PN-counter CRDTs (`a38e18c`)
- browser-durable storage via sqlite-wasm opfs-sahpool (ADR-0033) with Worker architecture, durable checkpoint, sign-out wipe, degrade path, and Playwright proof (`032e9c3`)
- merge unified-API Wave 2 — browser-durable opfs-sahpool storage (ADR-0033), no COOP/COEP (`1e61d14`)
- T6 attachments two-plane blob sync — pure core state machine, flutter + web drivers (ADR-0034) (`a659c44`)
- extend nostos-ffi-wasm to full typed Tier-1 surface (ADR-0034 Wave 4a) — port typed verbs from SyncClient onto the in-process ApplyEngine, add SqliteWasmStorage overrides (transactional enqueue_batch, dead-letter cols, counter merge), reuse nostos-domain CRDT, multi-table subscribe + resume (`34cc5a0`)
- merge Wave 4a (wasm full typed surface, ADR-0035) onto Wave 3 (T6 attachments); renumber 4a ADR 0034->0035 to resolve cross-worktree collision (`0082ccb`)
- flutter-web engine binding (Wave 4b) — WebNostosEngine over shared nostos-ffi-wasm worker, conditional-import platform switch, ADR-0036 (`5f3cc96`)
- close Flutter-web CRDT + atomic writeBatch gap (Wave 4c) via NostosSocket delegates that reuse NostosEngine + ship-if-open; Playwright browser smoke 2/2 green (`206dece`)
- complete @nostos-sync/web typed surface + attachments live-Worker gateway with browser tests (`e3bccb0`)
- add session presence index, PushNotifier port, typed column extraction (`32e5426`)
- add PgTokenStore with cairn_push_tokens schema (`80dab4f`)
- add SyncClient disconnect/resume with UniFFI exposure (`cec078b`)
- add off-hot-loop push enqueue with bench toggle and drop counters (`aad9c56`)
- add FCM, APNs, and Web Push provider rails (`1041cbc`)
- add push router with coalescing, offline expansion, and templates (`ad8401d`)
- add push-token REST routes and server push wiring (`b8d1437`)
- add flutter and node push token registration (`d00205d`)
- add push token registration to kotlin, swift, and dotnet SDKs (`8ee755c`)
- add react-native push registration and wake bridge (`1821756`)
- add experimental web push with storage seam and service worker (`e579f74`)
- extend capacitor plugin with beta push registration (`540c1ff`)
- add experimental live activities update rail (`3ae7140`)
- pilot nostos push in atlet with real-rail smoke harness (`de9ff2d`)
- atlet order-lifecycle push smoke leg on the real FCM rail (`82e07bc`)
- route visible FCM pushes through a HIGH-importance nostos channel for heads-up banners (`615610f`)
- foreground order-status banner posts a local heads-up on the nostos channel (`4154261`)
- iOS push leg — aps-environment entitlements, iOS 15 floor, atlet/notify foreground banner (ADR-0037) (`4fadbc3`)
- push arrival is felt — default sound + vibration (apns sound, android vibrate_timings + channel pattern) (`3b741b7`)
- action pushes — table:action:<category>:<title>:<body> rules; iOS aps.category, Android data-only wake, direct-APNs parity (`a0ca6da`)
- atlet action-push reference — iOS order_status category, Android native AtletMessagingService with Track/Dismiss buttons (`27d9f9b`)
- atlet flutter-web leg — wasm connect oneshot, worker schema/boot races, web push pilot SW, analytics web store (`ca41881`)
- web action-push parity — category buttons in atlet SW + webpush payload, localStorage bench-run store (`91fd111`)
- nostos-pushd daemon core — nostos-push crate with registry/auth/coalescer/rails (ADR-0038 wave 1) (`72b5397`)
- nostos push init|check — credential setup + reachability dry-run, docs/push.md (ADR-0038 wave 3) (`0c606ce`)
- RemoteNotifier delegation — nostos-server sends via remote nostos-pushd, contract 0.2.0 (ADR-0038 wave 2) (`192ee3c`)
- *(infra)* persist RemoteNotifier receipts cursor across restarts (NOSTOS_PUSH_REMOTE_STATE_PATH) (`4d30c27`)
- *(client)* client-apply bench leg — apply_bench example with honest drops/lag/checkpoint accounting (smoke-verified) (`682d0a3`)
- *(push)* v1.1 Postgres registry for nostos-pushd — PgStore behind pg feature, NOSTOS_PUSHD_DATABASE_URL (ADR-0038 addendum) (`c4cb53a`)
- *(bench)* real-Postgres ingest leg — bench_pg_ingest bin behind pg feature (smoke-verified, slot-clean) (`db296af`)
- *(push)* batch send POST /v1/send/batch — atomic phase-1, per-item results, contract 0.4.0 (v1.1) (`9a7dc63`)
- *(domain)* P5 sync streams slice 1 — :param placeholders in literal position, value-level bind_params, unbound Param never matches (p5-sync-streams-design Decision 2) (`551da4c`)
- *(infra)* P5 sync streams slice 3 — subscribe_stream/unsubscribe_stream frames, stream_error encoder, stream-tagged snapshot boundaries; transport rejects mid-session stream frames non-fatally until routing lands (`4eb6807`)
- *(domain)* P5 sync streams slice 2 — [streams] section in nostos_rules.toml, boot-time template validation in every mode, checksum participation; ActiveRuleset compiles templates once (Design Decisions 1-2); PUT /rules preserves streams like hand rules (`e9b1e4c`)
- *(infra)* P5 sync streams slice 4 — SnapshotSource::snapshot_stream port (bound PredicateExpr, not SQL text, across the boundary) + PgSnapshotter impl: compile_stream_where binds every value positionally, text values get the tenant clause's column-side ::text cast, tenant ANDed last from the principal (`4895788`)
- *(infra)* P5 sync streams slice 5 — mid-session subscribe_stream/unsubscribe_stream routing: register_stream binds params value-level, wraps rules+tenant fail-closed, caps streams against MAX_TABLES_PER_SOCKET, idempotent id replace, targeted stream snapshot with tagged boundaries; rules-reload check covers live streams; socket close disconnects stream sessions (`0e1f6ee`)
- *(server)* P5 sync streams slice 6 — boot log names loaded stream definitions (streams ride the existing load→compile→watch path unchanged) (`f91d076`)
- *(client)* P5 sync streams slice 7 — sync_stream(name, params).subscribe() with StreamHandle (drop-unsubscribes), mid-session command queue on the write-path notify pattern, active-set re-send on every reconnect, stream_error intercept; stream-tagged snapshot boundaries NEVER drive the table-scoped orphan-reap (subset bracket would delete out-of-stream rows) (`89a02ae`)
- *(flutter)* P5 sync streams slice 8 — frb subscribe_stream/unsubscribe_stream on the LIVE session (never replaces it), params as JSON-string (no-codegen trick), Dart syncStream(name, params).subscribe() PowerSync shape; web engine throws UnimplementedError (native-only v1); regenerated frb bindings (`922871a`)
- ADR-0040 accepted - capacity-shed continuity signal (NOSTOS_RESYNC_SIGNAL, default off) with client clear-and-reconcile handler; recovery e2e ignored pending convergence debug (`232c46c`)
- ADR-0040 implemented - writer hoisted before first-register (fixes first-connect snapshot deadlock above buffer depth), capacity-shed resync signal, client reconcile handler, recovery e2e green (`128baa9`)
- *(nostos-tauri)* production SDK surface — typed JS guest bindings, plugin config, push-token registration parity (`32d1a34`)
- *(nostos-push)* transient rail retry with re-debounce and terminal-only receipts (`644f681`)
- *(nostos-push)* hashed-at-rest API key store with per-tenant rate limits, Retry-After on 429s, key CRUD in nostos push CLI (`8a9be1e`)
- *(nostos_flutter)* NostosDatabase.local — the no-server entry point (`0b3ff54`)
- *(atlet)* no-credentials device round-trip harness — offline to online delta apply, green on iPhone and Android emulator (`1061df4`)
- *(server)* boot-time tenant-column audit names columnless and missing synced tables (`dcbff99`)
- *(infra)* tenant-scoped CRDT merge — ADR-0018 extended to the ADR-0030 paths (`2b54757`)
- *(flutter)* NostosDatabase.supabase() opens sessionless — sync starts at sign-in (`d57a855`)
- *(infra)* ADR-0041 accepted — iroh transport seam, dial-by-scheme, ws/iroh conformance parity (`71fecbf`)
- *(infra)* ADR-0041 D6 — native iroh session path, loopback bridge deleted (`43441c0`)
- *(infra)* NOSTOS_IROH_RELAY_URL self-host relay seam for the iroh transport (ADR-0041 D8) (`1f9fcdb`)
- *(client)* name the iroh-without-feature failure (ADR-0041 D7) (`31ad2de`)
- *(flutter-sdk)* iroh cargo feature + build-hook opt-in, off-default in shipped artifacts (ADR-0041 D7) (`9b5937c`)
- *(infra)* MirrorReplicator — channel-fed stream + in-memory snapshot buffer (B2 phase-1 seam) (`057f8d2`)
- *(server)* /ingest write door + NOSTOS_REPLICATOR=mirror — channel-fed sidecar topology (ADR-0042) (`929811f`)
- *(server)* NOSTOS_SYNC_AUTH=bearer — one shared secret resolves to one fixed principal (ADR-0010 addendum) (`760962f`)
- *(client)* non-retryable write rejections dead-letter on first attempt (ADR-0013 v2) (`a3d81a2`)
- *(client)* iroh_dial_check takes a seconds cap — the durable checkpoint prints mid-session for the D5 rig (`0014487`)
- *(tauri-sdk)* unified-verb completion — CRDT commands, deadLetters, connectionState, orSetTables/counterTables config (`b0be8f7`)
- *(security)* require jwt exp, validate nbf, add iss allowlist, bound jwks staleness, enable slot eviction by default (`ac78ae5`)
- *(security)* throttle failed admin-token attempts and add opt-in websocket origin allowlist (`b8b3d14`)
- *(security)* add opt-in metadata protection gating GET /schema on sync auth and GET /rules on the admin token (`a69014f`)
- *(cli)* nostos pull sends bearer token via --token/NOSTOS_TOKEN when server runs NOSTOS_PROTECT_METADATA=1 (`b56fffe`)
- *(cli)* nostos pull token hardening — flag beats env, redacted Display, refuse cleartext http to non-loopback unless --allow-insecure-token (`4744dce`)
- *(server)* keep 1 GiB NOSTOS_SLOT_MAX_LAG default, warn on 0, add doctor max_slot_wal_keep_size check (ADR-0043) (`be853dd`)
- *(bench)* record fan-out finish time in probe_10k and key ladder pass-2 on drop% < 1 (`de99ec1`)
- *(web)* Web Locks leader guard for OPFS multi-tab — secondary tab refuses connect unless opted in (`3faea07`)
- *(tauri)* tauri::test IPC fixture app, multi-table subscribe lift, conformance suite, readiness decisions (`958edb9`)
- *(tauri)* per-table where_sql via plugins.cairn.whereSql; resume_lsn stays stream-global; WebDriver rail deliberately skipped (`ae0630f`)
- *(web)* multi-tab follower proxy — losing tab forwards to the leader over BroadcastChannel, promoted on leader close; SharedWorker ruled out (`0028472`)

### Bug Fixes

- todo live-smoke target fails fast with setup steps when env.json absent; annotate operator-handoff status (`675db9c`)
- guard nostos_flutter build hook against non-code-asset phases (flutter run) (`9439e06`)
- *(sdk-e2e)* flutter slice to real test + harden kotlin/RN logcat capture (10/10 green) (`2a97a06`)
- nostos_flutter example generates UUIDv4 primary keys (`9b27df1`)
- *(infra)* bind timestamptz via chrono in PgWriteBack (created_at write bug) (`ef78c61`)
- *(server)* warn on NOSTOS_PG_URL without replicator=pg (snapshot-on-subscribe) (`6444c4f`)
- *(sdk)* reactive watch replays latest + IndexedStack tab-state preservation (`63658c9`)
- *(infra)* recover lost replication slots + slot-health probe (P0-1) (`911e340`)
- *(client)* merge offline patch writes + replay-on-top reconnect barrier (`9c47510`)
- *(sync)* snapshot-reconcile reaps offline-deleted orphans safely (ADR-0025 slice 1) (`2a351a5`)
- *(sync)* op-log tenant-tagging + JSONB payload read (real-PG bugs ADR-0025) (`5abc4fb`)
- *(sync)* tenant-scoped DELETE replay via REPLICA IDENTITY FULL + old row image (ADR-0025 F1) (`3974460`)
- *(sync)* close late-append oplog loss on shutdown; bail on PG_URL/replicator mismatch (ADR-0026) (`b0140d9`)
- *(flutter-sdk)* restore example host app so the W4 integration test can run (`fd7cdc7`)
- *(sdk-e2e)* require a booted simulator for swift; add SDK_E2E_STRICT mode (`429d41a`)
- *(client)* re-count pending on ack and dead-letter so redelivered acks cannot undercount (`c7a67dd`)
- *(flutter-sdk)* attach write-status pump at the later of first status read and subscribe (ADR-0027) (`997e236`)
- *(sdk)* derive swift harness paths and simulator UDID instead of hardcoding them (A9) (`b2f5480`)
- *(sdk)* capture kotlin logcat once so the verdict cannot race the proof lines (A9) (`3eb4b78`)
- *(sdk)* filter swift simulator discovery to iPhone/iPad and make the plans index authoritative (`b80afed`)
- *(sdk)* expose tauri subscribe command, repair malformed csproj XML, and fix react-native strict typecheck (`ce2a373`)
- correct fabricated github org to unfazed-dev and replace unregistered domain, entity, and contact placeholders (`50f87aa`)
- *(sdk-e2e)* stop pipefail SIGPIPE from failing successful proof-line matches and correct the 7-slice count (`1159092`)
- *(flutter-sdk)* correct invented NostosDatabase.supabase params in docs and gate doc snippets on the real signatures (`b76d608`)
- *(flutter-sdk)* forward rotated auth tokens into the live sync client so sessions survive token expiry (`e3736dd`)
- *(docs)* correct the ADR-0017 addendum — 1.4x not 2.6x required methods, scope the live-only write claim to NostosSocket, and say Err throws (`0b8754b`)
- *(infra)* bump slot_epoch at recreate so reconnect-resume gate fires (ADR-0025 slice 4) (`7e03608`)
- move powersync_role password out of migration into executor-agnostic provisioning script (`25de8b1`)
- guard queueDrain against pre-reconnect acks, make percentile return type explicit (`17f3fcc`)
- correct propagation_ms clock-offset sign (was true_delay - 2x offset) (`b7cdd7a`)
- lift extra JWT claims on the JWKS verification path (`d97bef3`)
- attach nostos adapter connection-state listener before subscribeTables (`4a108b4`)
- remove currentStatus backstop that overwrote correct connected signal (`10e396a`)
- normalize case-insensitive TRUE/FALSE keywords in scope tokenizer (`557c2ce`)
- reject control characters in sync-rules table names (`6158d15`)
- route FAB/spinner color through design token, correct detail snackbar copy, and fix StreamBuilder pop race (`6a7e7ef`)
- reframe Complete as log-and-clear per team-lead UX ruling (`0e79591`)
- make admin-auth audit test immune to ANSI-colored tracing output (`6a980d3`)
- stop CORS boot panic on non-empty NOSTOS_CORS_ORIGINS (C1) (`719fd11`)
- allow PUT in explicit-origins CORS methods for admin panel (`97a8bc7`)
- correct Wave 2 web-durability backend to opfs-sahpool per ADR-0017 (wa-sqlite was a misread of the ADR) (`2dce00c`)
- expose CRDT-table config on Flutter (native verbs were dead), add durable web reload test + Supabase round-trip scaffold (`4e3221b`)
- harden push security (path encoding, cross-tenant token guard, SSRF, caps) (`b24e16f`)
- clear flutter analyzer findings and format package (`38b88b9`)
- cross-compile android dylib via cargo-ndk with NDK toolchain env (`5a3df23`)
- compile capacitor plugin natives against capacitor 8 (`051d7c1`)
- gate webpush rail out of client SDK builds (`f01a41b`)
- wait for in-flight subscribe in nostos_flutter FFI session gates (`0805ad7`)
- prune FCM UNREGISTERED tokens across all live error body shapes (`4b4afe7`)
- ATLET_PUSH_PILOT must be =true (bool.fromEnvironment) and resume persisted session on cold start (`f73a25a`)
- coerce delete write params to declared column types (uuid-shaped text pks bound as Uuid failed with 'error serializing parameter 0') (`4253109`)
- *(sdk)* retry connection-level REST failures through the iOS local-network permission window (`91bf6db`)
- iOS push smoke green — local-network plist, drop notif-center delegate takeover, snackbar-aware taps (`c86f4be`)
- sweep stale sibling push tokens on register — reinstall rotations accumulate live-at-FCM rows (`6d689c1`)
- *(sdk)* push-token REST self-heals a stale session — 401 triggers one refresh + retry (`4c545be`)
- *(infra)* tenant-scope the subscribe-time snapshot — close the cross-tenant row leak (audit H1); also break the rules-channel busy-spin (L11) (`c017717`)
- *(infra)* replicator correctness — reinterpret xid unsigned (H2) and substitute unchanged-toasted columns from the old tuple (M2) (`416c935`)
- *(infra)* bound every lazy PG connect (pg_connect_bounded, M8), hold the write-back pool guard across CRDT merges (M4), timeout the JWKS fetch (M3) (`b51ba25`)
- *(server)* bail on NOSTOS_TENANT_COLUMN=id (M1 fail-open inversion), report a dead replicator driver as 503 on /healthz (M6), bound the shutdown op-log drain (M8) (`1d94519`)
- *(wasm)* drop the RefCell borrow before invoking the JS onChange callback (M5 reentrancy abort); correct the resume() doc (L3) (`e92cdf6`)
- *(push)* refund batch tokens on phase-1 failure and 400 above the effective burst ceiling; correct the advisory-lock and coalescer-retry comments (`b5f2003`)
- *(bench)* skip the trailing pace-sleep, drop the slot on early error paths, subtract pre-load contamination; apply_bench actually performs the router cross-check (`529aca9`)
- *(flutter)* normalize schema-qualified table names to the collapsed SQLite view name in Collection queries (ADR-0028 edge resolved) (`1e7dcf6`)
- *(infra)* gate pg_connect behind the pg feature — standalone no-feature cargo check -p nostos-infra linked tokio_postgres unconditionally (workspace CI stayed green via feature unification) (`2c1ea5a`)
- *(infra)* frontier-aware cursor for mid-session stream subscribe (`9f57dda`)
- *(infra)* stream unsubscribe removes only its session, never closes the shared socket sink; route stream frames live with sync_streams demo and regression test (`94406fd`)
- *(nostos_flutter)* export the AttachmentDatabase extension from the barrel (`9dc9060`)
- *(nostos_flutter)* export the NostosEngine seam from the barrel (`530eb88`)
- *(infra)* tenant-scoped snapshots skip tables without the tenant column — deliberately-global tables 42703ed and starved (`b7551ef`)
- *(infra)* snapshot query errors render their full source chain (`266edea`)
- *(client)* rejected subscribes surface as fatal errors and connected now means proven (`581b9bc`)
- *(cli)* nostos dev forwards CRDT column declarations from .env (`aa5ee27`)
- *(client)* reset_subscribed via mem::take — clippy if_not_else (`5ff6c1a`)
- *(sdk/flutter)* REST base keeps the sync URL path prefix — proxied sync keeps /schema and /push-tokens reachable (`ef3a40b`)
- *(server)* tenant column resolves under bearer auth — fully-offline doorbell hints regain their tenant (ADR-0010 addendum) (`0a322af`)
- *(tool)* d5 field-leg script builds the server with the iroh feature (`f177270`)
- *(tool)* d5 scrapes the dial_url field, not the banner prose (`f50da88`)
- *(tool)* d5 keeps the ticket= value past the first equals (`060b804`)
- *(tool)* d5 rig carries its own all-mode rules file (`80ca3ab`)
- *(tool)* d5 rules file carries the required version field (`90e4c82`)
- *(deps)* clear the h2 DoS advisory and two yanked crates; allow MPL-2.0 for ece (`0cab582`)
- *(infra)* static bearer digest compares arrays, dropping the deprecated GenericArray as_slice (`648815e`)
- *(deny)* allow Unlicense and ignore two transitive unmaintained advisories — CI checks --all-features (`b63bc05`)
- *(release)* grant contents/pull-requests write to the two jobs that publish — repo default is read (`4984e63`)
- *(sdk-e2e)* make the dotnet slice run on linux — portable mktemp, .so copy rule, LD_LIBRARY_PATH (`fc36ad3`)
- *(sdk-e2e)* build and install nostos_node.node in the node slice instead of loading a stale gitignored addon (`8dd71b1`)
- *(release)* replace the placeholder _comment when filling prebuilt.json so the published manifest is not self-contradicting (`51561c6`)
- *(ci)* require a 5-poll streak for postgres readiness — the publication appears on the init server, not the real one (`5d4ed36`)
- *(store)* never hold a dashmap shard guard across an await — the fan-out deadlock behind the 6h ci hangs (`e5c4579`)
- *(domain)* bound predicate parser depth and token count — deep where_sql aborted the process (`814b8e2`)
- *(infra)* cap inbound websocket message and frame size (`fe53ec3`)
- *(infra)* back off jwks refetch during endpoint outage instead of once per request (`0e221cd`)
- *(server)* keep bearer tokens out of request spans and make static bearer compare constant-time (`8f03405`)
- *(infra)* apply the session predicate on op-log replay — resume bypassed the ruleset (`6ec5525`)
- *(infra)* apply the rules scope to stream snapshots — the first sync bypassed it (`4ae4259`)
- *(server)* refuse to boot anonymous sync on a non-loopback bind (`52f1561`)
- *(infra)* clamp client acks to what the sink actually delivered (`c856129`)
- *(client)* unpublish bench_apply so it stops breaking fresh-slot e2e tests (`e27bc2e`)
- *(client)* bench teardown must not panic or batch its two cleanup statements (`acea45f`)
- *(write-back)* collapse the cross-tenant rejection into one non-disclosing message (`c930997`)
- *(sync)* refuse over-cap snapshots loudly and bound live sessions per principal (`4b7386d`)
- *(domain)* make predicate evaluation three-valued so Not over an absent column stops over-delivering (`0abeee0`)
- *(auth)* enforce nbf and issuer allowlist on HS256 path, add tests for both verifiers (`899530e`)
- derive REPO_ROOT from git toplevel in kotlin/rn e2e scripts instead of hardcoded developer_ssd path (`92001dd`)
- set SDKROOT in nostos_flutter build hook so ring compiles under the Xcode script-phase env on macOS (`3254019`)
- treat empty or bare-name SDKROOT as absent in nostos_flutter build hook (`444c432`)
- *(bench)* make probe_10k undelivered breakdown exact per event instead of integer events-per-client (`22aa28a`)
- *(web)* ship the wasm core in the @nostos-sync/web tarball — wasm-pack's pkg-web/.gitignore was silently excluding it; check-pack.cjs guards it (`ee77b60`)

### Refactor

- /demo uses NostosSocket + the engine readback API (`74eaca1`)

### Documentation

- note localStorage best-effort durability in Show HN draft (advisor-flagged release-notes hygiene) (`7396fc8`)
- refresh RESULTS.md headline to fresh 833k/208× measurement (post-C3) (`d7529e0`)
- refresh RESULTS headline to fresh 833k/208× measurement, fix C3 attribution, sync ROADMAP + launch drafts (`06481e4`)
- launch-readiness sweep - security model, README refresh, plans, env examples (`04006b2`)
- record F5 typed-payload research verdict in gap list (`90dde26`)
- nostos-client Rust SDK README, parity plan bump to 4/10 (`8af12da`)
- correct platform count to 7/10 (nostos-ffi-wasm + @nostos-sync/web is one platform) (`2fe3d12`)
- elevate Kotlin SDK to device-verified (API-34 emu E2E green) (`d89c4f0`)
- mark SDK-E2E spine + Rust + Flutter done (3/7), queue FFI fan-out (`14ccc4e`)
- resolve Supabase cloud IPv6 via WARP relay + flag fresh-subscriber snapshot gap (`721a056`)
- add flutter-redesign, cloud-trust, AI-roadmap, coverage plans (`c4db580`)
- *(example)* full Supabase chain topology + invalidated-slot recovery (`d4b1b56`)
- *(plans)* sync-strategy analysis — single offline-first strategy, per-field tier is the seam (`4641116`)
- *(adr)* ADR-0025 persisted op-log backfill design + plan (`f1e8548`)
- *(sync)* close min(acked_lsn) compaction deferral as intentional, not a gap (ADR-0025 F3) (`73ea2c0`)
- add OPERATING runbook + ADR-0026, refresh QUICKSTART watch-bug + moat numbers (`96f4a30`)
- add 2026-07-29 project completion assessment with claim list (`e043bd3`)
- record firehose-saturation finding and A2 wedge post-mortem in assessment addendum (`64825b1`)
- scope the A10 CPU measurement to server-side load and note bench ignores NOSTOS_FAKE_* (`f319ace`)
- record A7 drift fix and correct the sdk-e2e strict-mode slice count (`6908bd9`)
- record A9 closed with 10/10 sdk-e2e slices passing in strict mode (`bf4715c`)
- add plans index classifying all 23 plans and flag the three that misdirect (A8) (`5310ef5`)
- close C15 with per-SDK publishability evidence and correct the launch draft's SDK status (`3edbf58`)
- *(sdk)* correct uniffi-bindgen regeneration commands and cite tauri camelCase arg conversion (`034b1fb`)
- *(sdk)* scope the csproj evaluation claim to hand-declared deps and drop the unverified SIGPIPE asymmetry story (`c6c7e98`)
- adopt ADR-0028 rejecting materialized typed tables and rewrite the flutter redesign plan as an as-built record (`7ea15f4`)
- *(api)* add a per-SDK API reference with source-cited signatures and gate the flutter page on the real factories (`fd6ff68`)
- *(api)* explain why SDK read models and reactivity differ, citing the crate graph and ADR-0017 (`31b80f4`)
- correct six ADR and roadmap status lines that understated shipped work and add the completion audit (`f0f2cdf`)
- *(adr-0017)* reject the IndexedDB mirror and record that the browser SDK is live-only, not merely non-durable (`3efb761`)
- add multi-SDK pomodoro fixture matrix plan and four workstreams (`c69cb8b`)
- *(plans)* design the WS4 clear() trait contract that WS1 must marshal (`e1bed3c`)
- *(plans)* record verified WS2 finding — reactive channel exists, native SDKs are leaf binding (`0c2d1d5`)
- *(adr)* add ADR-0029 (sign-out/wipe) and ADR-0030 (CRDT, proposed); record design-wave outcomes (`b5dade8`)
- *(adr-0030)* ratify client+server HLC, record piece-1 shipped + slice plan (`78bb158`)
- *(adr-0030)* record D7 bench before/after — gate passed (off-path, 0.00% drops, -1.3% within noise) (`96f837c`)
- *(adr-0030)* mark all CRDT slices shipped, both real-PG e2e green (WS3 complete) (`a828cce`)
- *(adr-0029)* record Flutter signOut + HS256 exp enforcement status (WS4 audit) (`3fda345`)
- correct ADR status to shipped state and record 2026-08-04 status audit (`756de2b`)
- add remaining-work-2026-08-05 report; correct ADR-0029 D4 live-socket close + audit drift (`1b61733`)
- *(adr-0029)* ratify full-wipe as v1 cross-principal isolation; defer per-principal outbox (inert as specified, weakens isolation if made meaningful) (`18b2b87`)
- retire cross-stage 208x/417x moat multiples for honest same-units framing (`61719dd`)
- annotate atlet design provenance with source-state ruling (`ba232d0`)
- add sync-streams suite implementation plan with operator-ratified rulings (`e000621`)
- record ratified NOSTOS_ADMIN_TOKEN shape in sync-streams Task 21 (`566c313`)
- commit sync-scoping and powersync research docs cited by the stream-suite plan (`5d932da`)
- add ADR-0031 for three-mode sync rules and checksum resync (`52646f8`)
- align ADR-0031 reload and checksum wire-field with D2/D3 rulings (`78c79d8`)
- *(scope)* note Claim always resolves to text on the type itself (`04850f7`)
- record flutter pilot conformance results and freeze adapter spec v1 (`5502f62`)
- drop unimplemented updateSession from adapter spec, bump v1.1 (`9afd3dc`)
- *(atlet)* document db_bytes measurement scope + REPLICA IDENTITY caveat (`ec7c1be`)
- document the three-mode sync-rules suite (`9f33cc4`)
- ratify tier-1 SDK extension (v1.2) and add unified API implementation plan for executor agents (`9c6cc28`)
- assign T4 CRDT to Wave 1; correct write list (patch/atomic writeBatch; retry/discard v1.1-deferred) (`8fbce46`)
- correct Wave 4 premise (wasm was apply-engine-only) + 4a/4b re-scope in contract and implementation plan; note frb.web rejection (`527c597`)
- record kimitail audit findings (38 findings, net -1209 lines) (`06e8c77`)
- add ADR-0037 sync-aware push notifications with research and plan (`0b877b9`)
- tick push plan 1.1-1.2/1.4/5.1, close ADR-0037 extraction hole (`a6246f8`)
- tick push plan 1.3/2.1-2.3, amend ADR-0037 offline doorbell (`b99736d`)
- tick push plan 2.4, 3.1-3.3, 4.1-4.2 (`36c904d`)
- tick push plan 5.2-5.4 and 6.1-6.4 (`8d6ff3e`)
- ADR-0038 nostos-pushd standalone push daemon + launch-blocker implementation plan (`76c00aa`)
- wave-0 pins + nostos-pushd OpenAPI contract (ADR-0038) (`c358c46`)
- pin wave-2 delegation amendment — registry-free rail mode via optional platform (0.2.0) (`4eaf1a7`)
- nostos-push README — what nostos-pushd is and is not (ADR-0038 wave 4.4) (`bf2351d`)
- ROADMAP phase-3 push deliverable + launch narrative (ADR-0038 wave 4.4-4.6) (`93b8026`)
- security audit verdict + same-day fix batch scope (wave 4.1) (`1751e81`)
- tick docker leg — image build verified with nostos-pushd (wave 4.3) (`63add82`)
- migration guides from PowerSync and Realm (Phase 3 deliverable) (`ecbfcc7`)
- retire stale ≥5× ratio framing from benchmark methodology §1 (mirrors 2026-08-06 Correction) (`dc717fc`)
- AGENTS.md e2e verb gains required --test-threads=1 (snapshot/concurrent within-binary race) (`ef813f2`)
- correct stale QUICKSTART known-gaps, plans/README reconnect row, and in-memory-Patch / push-flush comments (`12ba47f`)
- *(audit)* marker-sweep audit 2026-08-17 — 23 verified findings with same-day resolution appendix (`36c86bd`)
- *(plans)* P5 sync-streams design draft (server-defined named streams, value-level param binding) (`9a81169`)
- *(api)* disclose intra-batch duplicate-token coalescing in the batch contract (`6c95fad`)
- *(adr)* P5 sync streams slice 10 — ADR-0039 (server-defined client-parameterized streams, value-level binding, fail-closed tenant AND-wrap, JOIN/CTE ceiling + ponytails); parity-plan P5 row flips to v1-parity; design doc marked IMPLEMENTED with checklist ticked (`4bdb4a1`)
- *(adr)* 0039 consequences — stream e2e ran live 2026-08-18, all 5 green incl. cross-tenant gate; full serialized pg suite green (`a67e2da`)
- reconcile parity-plan P5 shipped status, map nostos-license and nostos-cli in crate maps (`f1813eb`)
- land crate-map rows for nostos-license and nostos-cli in CLAUDE.md (AGENTS.md symlink target) (`664e81e`)
- record Show-HN precondition in RESULTS.md — real-PG client-apply end-to-end number pending (`caecb3b`)
- residue sweep - ADR-0040 slow-client policy proposal, router parked on full-path evidence, AGENTS.md carries real-PG number, ponytail ledger harvest (`6ab36ae`)
- add plan for sdk_tauri and sdk_flutter fixes plus nostos-push productionization for arxa integration (`ea29c09`)
- *(nostos-push)* document known Web Push limits (killed-tab no-data-wake, app-owned resubscribe) (`66659ed`)
- *(nostos_flutter)* versioning and tag-truth sections, atlet reference wiring with CI coverage (`580796e`)
- *(adr)* ADR-0041 transport abstraction — ws|iroh dial layer, transport-only scope guard (`a2f61bb`)
- plan status addendum — M7 minting seam closed, send path mapped, ADR-0041 pending (`d5721bb`)
- *(adr)* ADR-0041 decision memo — accept/reject packet for spike/iroh-transport (`f08fee4`)
- *(adr)* ADR-0041 memo — spike re-run green, merge rehearsed, mobile build viability verified (`a3fc21f`)
- *(adr)* ADR-0041 accepted — conditions recorded, gated items D5-D8 tracked (`0b1ee96`)
- *(adr)* ADR-0041 condition 2 resolved — D6 native iroh session path (`b45da5b`)
- *(operating)* iroh relay self-host guidance + n0-fleet privacy note; ADR-0041 condition 3 resolved (`636edec`)
- *(infra)* correct the NOSTOS_IROH_RELAY_URL env-only rationale comment (`ce55c89`)
- *(plan)* D7 closed — Flutter SDK iroh wiring landed, off-default posture kept (`d1ed6a1`)
- *(adr)* 0042 verified end-to-end; wire facts for phase-1b clients (`3d6fb68`)
- integration-plan gap list updated to shipped state; D5 field-leg runbook + desktop automation (`d0cc2f4`)
- *(plans)* track A status recorded — unified verbs complete, rail green, publish+device gate remain (`f442c2e`)
- v0.2.0 release handoff — push, manifest PR, pub.dev, kit pin flip (`73b5793`)
- *(plans)* v0.2.0 red-rail findings — advisories, MPL-2.0 policy, sdk-e2e deprecation, perf-floor flake (`be8fa1e`)
- *(plans)* release handoff gains the pre-publish pull gate and the kit-pin correction (`3b6f0bc`)
- *(plans)* record the --all-features cargo-deny gap and the missing release permissions (`a6a0667`)
- *(plans)* record the three macos-only assumptions that kept the dotnet slice red (`18ffd5a`)
- *(plans)* record the stale-addon node slice failure and the sdk lockfile drift (`9460a25`)
- *(plans)* record the dashmap-guard deadlock, its repro, and the 40/144 vs 0/144 differential (`dbe274d`)
- *(plans)* record the v0.2.0 security audit — findings, fixes, and open gaps (`1a0a792`)
- *(plans)* record the replay authorization leak and the stream-snapshot gap (`3a06dbb`)
- *(plans)* record verification results and pre-existing pg snapshot failures (`eb33dfa`)
- *(plans)* attribute pg snapshot failures against the pre-session baseline (`1b67370`)
- *(plans)* correct the ack finding, record the stream-snapshot fix and boot guard (`9958c2d`)
- *(plans)* record the pg root cause and what was deliberately left (`b7ea83a`)
- *(plans)* record the re-verified make ci and boot-guard results (`bdcfe13`)
- *(plans)* record the orphaned-slot hazard that faked a fan-out regression (`6416523`)
- *(plans)* record the full pg e2e suite green, retiring the 2-failure caveat (`b8f20ea`)
- *(plans)* plan the close-all-findings pass and correct two audit pointers (`005c1e7`)
- record the fix-all pass outcomes, verification runs, and the two carried-forward exclusions (`bcd2d37`)
- note that the websocket origin allowlist does not cover the iroh transport (`d527d04`)
- record the green full-gate result (`41f2b9c`)
- record stress and pressure results plus the benchmark contention caveat (`2f3d6ca`)
- record the real bench binary names and the unrun 10k soak (`4a5a9fa`)
- record nostos pull --token/NOSTOS_TOKEN follow-up as done in b56fffe (`b828f46`)
- *(cli)* note ps visibility and cleartext caveats for nostos pull --token (`834303f`)
- record code cross-check verdicts and finding-4 closure in 899530e (`45e85b1`)
- *(audit)* record NOSTOS_SLOT_MAX_LAG decision (ADR-0043) as closed (`cdc7079`)
- record 10k soak and throughput re-measure still host-blocked on 4 orphan context-mode procs (`39e889e`)
- *(adr)* note 1 GiB slot-lag threshold is unmeasured, name revisit trigger (`f18607e`)
- ADR-0044 cargo build-dir on external SSD, dep debuginfo off, weekly mtime sweep (`2347780`)
- ADR-0044 sweep prunes whole idle workspace dirs, never single files (`288050c`)
- ADR-0044 record verification of nostos_flutter rust build-dir and IDE gradle daemon on external volume (`f2b9d2a`)
- *(bench)* mark scale ladder as eval-only, not comparable to PowerSync figures (`495af19`)
- plan for shared full-path Nostos vs PowerSync harness (`89e7fcf`)
- record grilled decisions for the PowerSync full-path harness plan (hold, 100/500/1k ladder, MongoDB Step 0) (`6307e49`)
- mark finish-time probe and drop% pass-2 done with the validated 20k/300 run and correct the ladder breakdown figures (`cfecca6`)
- numbers table with host load1 columns and the INVALID first diagnostic attempt (`b5dd15c`)
- run 1 (100k x 500, valid) falsifies kernel TCP pressure and swap; next run is the original 5000-event shape (`6ff274b`)
- record run 1 100k and 50k control results in fan-out cliff diagnosis plan (`ba874d3`)
- run 2 attempt 1 marked INVALID (host contention mid-run); partial numbers kept (`9132089`)
- headroom rule caption (start gate vs mid-run non-harness CPU) and attempt-1 trace marked suggestive (`16bed3b`)
- run 2 attempt 2 INVALID (sustained non-harness CPU); diagnosis blocked on quiet host (`cad8454`)
- one-page plain-English infographic of Nostos and the 2026-09-02 scale tests (`a389e4e`)
- add inline SVG charts to the plain-English one-pager (speed by tier, loss per 1k, before/after fix) (`0fb789f`)
- tighten one-pager — larger type, fewer words (`67eef40`)
- one-pager honesty pass — authoritative 10k figure, missed-not-lost framing (`a9cc50d`)
- one-pager prints as one page (custom @page); chart-3 title covers both fixes (`2a7407a`)
- phase 3 launch readiness audit — crates.io name collisions, absent credentials, stale v0.2.0 tag, two packaging bugs (`d0e4cd4`)
- launch audit second pass — maven publishing resolved and verified, io.github namespace defuses the domain gate, v0.2.0 re-cut locally (`d1b942e`)

### Performance

- fan out sequentially instead of spawning one task per session per event; probe waits for subscribe quorum (`4bba0fb`)
- share one Arc<ReplicationEvent> per fan-out instead of cloning per session; 10k soak on Linux meets <1% drops (`a7bcd91`)
- *(fanout)* split the delivery walk across cores above 8k matched sessions (2x with real sinks, measured by the new nostos-fanout-walk probe) (`800e09b`)

### Styling

- cargo fmt (nostos-ffi-wasm, nostos-infra examples, client e2e) (`fe6932c`)
- inline format args in e2e_client_reconnect_replay (clippy -D warnings) (`b5a703d`)
- rustfmt drift from tenant-CRDT commits (`6bb730d`)
- clippy pedantic cleanups — fn-pointer take, nested or-pattern (`45c3aa2`)
- rustfmt the dial-check example cap (`ef88c70`)
- dial-check cap follows the pedantic match shape (`79c0d62`)
- rustfmt the any-collapse test assertion (`7958877`)
- rustfmt the security-hardening pass (`3c2d76a`)
- allow excessive bools on the clap config struct (`dab4360`)
- *(flutter)* dart format with Dart 3.13 (sdk + atlet); CI checks format from now on (`783fd4d`)

### Testing

- nostos-client live-replication E2E vs spine (reference SDK shape) (`792a6c8`)
- node SDK live-replication E2E vs spine (smoke_live.cjs) (`eb3913d`)
- *(sync)* real-PG op-log replay + restart-resilience e2e (ADR-0025 slice 6) (`b5d63f0`)
- *(sync)* slice-6 e2e harness fixes — query_one-on-empty, seed-cleanup, tracing init (`30b8f4a`)
- *(sync)* slice-6 real-PG e2e green — replay + aged-out verified (`52abb37`)
- *(client)* real SyncClient persists server epoch from resume_info (ADR-0025 F2 hardening) (`ffc97d4`)
- *(client)* real SyncClient reconnect-replay e2e incl. delete (ADR-0025 #2) (`06fe960`)
- *(fixture-todo)* add delete scenario, live CRUD integration coverage, repository port (`2e5d7bc`)
- *(nostos-client)* make change_notifications deterministic by latching the pump on subscribe and disabling quiesce (`6d91a29`)
- *(infra)* real-PG write-amp harness (ADR-0025 slice 6) + live-Supabase JWKS cold-stranger probe (`0a52b79`)
- verify shop product asset actually resolves via rootBundle (`ad0cc75`)
- cover fresh-ruleset mid-session subscribe and watch_rules notify semantics (`c8c74b1`)
- prove sync-rules mode-switch truth transfer (`1f211b4`)
- pin SDK attachment wire contract to nostos-core in CI (`3c08f9a`)
- pg e2e re-entrancy — document required --test-threads=1 in AGENTS.md, truncate typed_probe fixture (`ece02a7`)
- *(infra)* typed_probe fixture create-before-truncate for freshly seeded DBs (`94b0e72`)
- *(infra)* P5 sync streams slice 9 — e2e_pg_sync_streams.rs covering §6 items 1-6 (lazy add + targeted snapshot, unsubscribe stops flow, cross-tenant abuse gate, two-stream dedup, reconnect re-snapshot, rules-denied non-fatal stream_error); compiles + self-skips clean — live-PG execution blocked by Docker Desktop VM corruption (container FS read-only, API 502) (`94f5e7f`)
- *(infra)* P5 e2e live-PG fixes — collector decodes type-less row frames + hex payloads, UUID-typed org_id binds, tracing init, post-unsubscribe insert uses the exact-matching title; all 5 stream e2e + full serialized pg suite green (slots pruned after) (`b7a2cd6`)
- real-PG client-apply e2e throughput harness - 34.8k-36.2k rows/sec full path, zero-drop at 32768 buffer, 29.9% drop finding at default 1024 (`d08cb2c`)
- *(nostos-tauri)* conformance suite ported from adapter contract v1.1 + ACL lockstep pin, tauri slice wired into CI (`88ca3c6`)
- *(flutter)* integration harness compiles server first with tunable budget and isolates ambient nostos_rules.toml (`4562c8d`)
- *(infra)* ADR-0040 reorder regression — oversized snapshot drains, cap reject closes 1000 (`e7ac28f`)
- *(infra)* ack fixtures must deliver before acking, not simulate progress (`5135315`)

### Miscellaneous Tasks

- release pipeline for CLI binaries, Flutter prebuilt artifacts, and glue-crate gating (`60b5644`)
- *(flutter-sdk)* remove redundant example app, lift W4 test into SDK, add USAGE guide (`5042928`)
- ignore .kimi-code and .headroom agent scratch dirs (`27d90ff`)
- run host SDK live-replication e2e slices (rust, node, tauri) (`f26831f`)
- drop tauri from sdk-e2e job (webkit system deps); note flutter slice run-dir and frb stale-dylib traps (`c7942b2`)
- *(sdk)* package all nine SDKs at 0.1.0 with LICENSE, repository metadata, and READMEs (`d98c62f`)
- *(archive)* move Flutter example+tests to archive/, drop flutter sdk-e2e slice to 9 (`76ed6ee`)
- remove superseded Flutter fixtures (greenfield per plan D0) (`2489ffb`)
- lockfile for push router and REST route dependencies (`5f4fa8b`)
- *(infra)* silence non-pg dead-code warnings in oplog (scoped clippy clean) (`387841b`)
- *(flutter)* bump nostos_flutter to 0.2.0-dev.1 and record post-0.1.0 drift in CHANGELOG (`8e80d74`)
- *(flutter)* frb regen — doc-comment sync only, wire glue byte-identical (`34f992f`)
- cap all 15 jobs with timeout-minutes; ci.yml supersedes concurrent runs (`19c85a2`)
- gitignore python bytecode from packaging/release tooling (`4a958ed`)
- bump workspace version to 0.2.0 and align homebrew formula (`73a0444`)
- *(flutter)* refresh the glue-crate lockfile to the 0.2.0 workspace versions (`93faf95`)
- *(flutter)* sync atlet pubspec.lock to nostos_flutter 0.2.0 (`a99c43f`)
- pre-create SwiftPM SourcePackages dirs before atlet pub get (Flutter 3.47 rsync bug); flutter-version 3.47.5 (`33f0fcd`)
- toolchains track latest stable (rust 1.98 channel, flutter 3.47.5, frb 2.13.0); clippy 1.98 fixes; pg-e2e slot note (`81f4d21`)
- make pg-e2e — sweep leaked inactive e2e_* slots, then run the real-PG suite (`a03c710`)
- *(bench)* linux bench containers move to rustc 1.98 (workspace MSRV); build failures now print the real cargo error instead of a bogus exit 92 (`9179942`)
- *(sdk)* align every shipping package to the 0.2.0 release train; @nostos-sync/node marked private (self-described feasibility scaffold) (`2b8cf89`)
- *(kotlin)* maven-publish + signing + a centralBundle task — produces a verified 30-file Central Portal bundle; group id stays a -P override so the namespace decision is not baked in (`92950a8`)

### Other

- Revert "docs: refresh RESULTS.md headline to fresh 833k/208× measurement (post-C3)" (`a442ece`)
- Merge branch 'worktree-dart-dev-api-abstraction': Dart reactive facade (ADR-0024) (`15a320c`)
- Merge branch 'worktree-agent-a829d134a1183217c' (`3601af4`)
- nostos-pushd stack service + image leg (ADR-0038; builds once wave-1 crate lands) (`24dd527`)
- push on/off clean pair at 1k — hot loop unchanged with delegation code in-tree (wave 4.2) (`d1e9c58`)
- nostos-pushd audit fix batch — rail-role keys, rate limits, 409/0o600/oracle closes, contract 0.3.0 (wave 4.1) (`5b25ffc`)
- real-PG ingest + client-apply legs — MEASURED sections, paced apply mode, production quiet-window runs (`618c37c`)
- v1.1 review fixes — bounded PG handshake/statement timeout, symlink-safe cursor tmp write (`b523c21`)
- *(iroh)* ADR-0041 transport seam over iroh 1.1 — dial-by-scheme, bridge accept loop, ws/iroh conformance parity (`22bc108`)
- *(flutter)* 0.2.0 — first flutter-carrying tag; changelog final, prebuilt manifest version aligned (`05bb0d1`)
- record 2026-09-02 throughput re-measure as host-contended and aborted; baseline unchanged (`cc00d04`)
- record run2 re-measure as host-contended (VS Code Code Helper); baseline unchanged (`ec824e5`)
- re-measure throughput (3 passes) and 10k soak after clearing host contention (`be4c8f0`)
- native 3-pass re-measure on fixed fan-out (median 2,618,601 ops/sec, 0.00% drops) and 10k soak; update headline (`0b7fd3b`)
- probe takes a listeners arg (N loopback addrs), raises nofile to hard limit, reports completed+peak RSS; add scale-ladder.sh (`1dc27b2`)
- scale ladder 20k-100k on Linux container + macOS 10k re-measure and soaks (`c5929b9`)
- make probe subscribe-quorum wait progress-based and report late subscribers; add 30k-100k rerun script (`d67e573`)
- re-run 30k-100k ladder with progress-based quorum (`2608a2a`)
- add 100k fan-out cliff diagnostic (probe progress line + in-container VM sampler) (`498a4fc`)
- gate fan-out diagnostic measurements on host headroom (load1 < 8, no other bench container, lock free) (`07d587b`)
- probe reports whether the fan-out loop returned (replicator budget exhausted) vs still running at window end (`38dd0ea`)
- fan-out diagnostic takes tier specs from argv; inner tier exits 0 after sampler teardown (`9ee9b10`)
- raw logs for the 100k fan-out cliff diagnosis (run 1 valid; run 2 attempts 1-2 INVALID, host-contended) (`d828b6e`)
- 100k fan-out cliff attributed to the single-threaded sequential loop — gated run falsifies TCP pressure, swap and CPU saturation (42% idle at 11.4x collapse) (`fceb5b3`)
- retract the 100k cliff attribution — the fan-out walk measures LINEAR to 100k (0.458 to 0.486 us/delivery) and is ~1.4% of the observed per-event cost (`5b41723`)

---

🤝 Collaborated with Claude via [Claude Code](https://claude.com/claude-code)
