# --- Rust ---
/target
**/target
**/*.rs.bk
Cargo.lock.bak

# Per-crate target dirs (defensive)
crates/*/target

# --- Benchmark artifacts (regenerated) ---
# Keep the canonical benches/results/ committed (the headline deliverable), but
# ignore scratch per-tier dirs created during exploration.
/benches/results-*
/benches/r-*
/benches/out
target/criterion

# --- IDE / editor ---
.idea/
.vscode/
*.iml
*.swp
*.swo
*~
.DS_Store
nbproject/

# --- Environment / secrets ---
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# --- OS ---
Thumbs.db

# --- Docker ---
docker/data/

# --- Coverage / profiling ---
tarpaulin-report.html
flamegraph.svg
perf.data*

.claude/
.claude-flow/
.kimi-code/
.headroom/

# --- Agent-Native local plan bridge (machine-local token/state, never commit) ---
plans/*/.plan-url
plans/**/.plan-url
plans/*/.plan-state.json
plans/**/.plan-state.json

# --- RN-iOS host/example app (verification vehicle, not committed) ---
# Heavy (node_modules, ios/Pods, build). The SDK example dirs were archived
# (ADR: sdk-e2e 10→9); this is a local-only harness for iOS TurboModule verify.
sdk/nostos_react_native/example/

# --- RN-iOS TurboModule: vendored nostos_swift artifacts (regen from sdk/nostos_swift) ---
sdk/nostos_react_native/ios/nostos_swift.swift
sdk/nostos_react_native/ios/ffi/
sdk/nostos_react_native/ios/libnostos_swift.a

# --- Local MCP wiring + Supabase CLI scratch (machine/project-local, never commit) ---
# .mcp.json pins this checkout to one operator's Supabase project; supabase/.temp/
# is CLI scratch. Neither belongs in the shared OSS repo.
.mcp.json
supabase/.temp/
