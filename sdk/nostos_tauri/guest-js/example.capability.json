{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "nostos-fixture-capability",
  "description": "Example capability file for a Tauri app using the nostos plugin: grants the plugin's default permission set (all ten commands). Split into scoped per-table sets when the plugin publishes them.",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "nostos:default"
  ]
}
